---
phase: 07-rollout
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - .planning/rollout-contacts.csv
  - .gitignore
  - .planning/phases/07-rollout/07-USER-SETUP.md
  - docs/CUTOVER.md
autonomous: false
user_setup:
  - service: supabase-auth-smtp
    why: "Replace Supabase's 2/hr default SMTP with Resend before any production-bound auth email (password reset, email change, magic link). Without this, any password-reset flow at rollout time silently 429s after two attempts."
    env_vars: []
    account_setup: []
    dashboard_config:
      - task: "Configure custom SMTP in Supabase Auth"
        location: "Supabase Dashboard → Project tgswsdfaszvztbpczfve → Auth → Settings → SMTP Settings"
        details: |
          Host: smtp.resend.com
          Port: 465 (SSL) or 587 (STARTTLS)
          Username: resend
          Password: <RESEND_API_KEY> (the same key already in Vercel for SLA emails)
          Sender email: same as SLA_ALERT_FROM_EMAIL
          Sender name: Paratus Group Dashboards
          Save → click "Send test email" to your own inbox to verify deliverability.
      - task: "Add accept-invite redirect URL to Auth allow-list"
        location: "Supabase Dashboard → Auth → URL Configuration → Redirect URLs"
        details: "Add https://paratus-group-dashboards.vercel.app/auth/accept-invite (production) — preview previews already use the wildcard from Phase 1."
    local_dev: []
  - service: github-paratusgroup-org
    why: "Repo handover requires a destination GitHub org under the master Google account so we can transfer ownership without losing CI/CD or Vercel-GitHub integration. Created here, the repo transfer happens in Plan 07-04."
    env_vars: []
    account_setup:
      - url: "https://github.com/account/organizations/new"
        skip_if: "Org `paratusgroup` already exists under para.group.n8n@gmail.com"
    dashboard_config:
      - task: "Create the paratusgroup org"
        location: "github.com → New organization → Free plan → Owner = para.group.n8n@gmail.com"
        details: "Name: paratusgroup. Adding William or a Paratus IT contact as Owner is a Plan 07-04 task — the org just needs to exist now."
    local_dev: []
---

<objective>
Run the pilot cutover for Mozambique end-to-end. Three gated stages: (1) collect Williams' answers to the eight open questions and finalise `rollout-contacts.csv` for Mozambique, (2) wire Resend-as-Auth-SMTP + run `provision-users.ts --country=MZ` against production Supabase, (3) smoke-test from agent + admin seats, flip the form-side ingestion path to production, and start the 24-48 h soak (validation in Plan 07-04).

Purpose: This is the live pilot ceremony — the first time real Paratus humans touch the production system. Every gate is a checkpoint that requires William or Gerhard in-the-loop. Once Mozambique passes the soak, Plan 07-04 mechanically extends the same posture to the other eleven countries.

Output:
- `.planning/rollout-contacts.csv` — real CSV (gitignored) with Mozambique rows for the pilot, plus rows for the other 11 countries staged but un-provisioned (Plan 07-04 runs them)
- `.planning/phases/07-rollout/07-USER-SETUP.md` — captures William's answers to the 8 open questions and the Resend-SMTP / GitHub-org setup checklists
- `docs/CUTOVER.md` Mozambique section — checked/dated/signed where applicable; updated as live observations come in (NOT pre-populated)
- Live infrastructure changes: Resend wired as Auth SMTP, accept-invite redirect URL added, `paratusgroup` GitHub org created, ~3 Mozambique users provisioned, Path 1 or Path 2 ingestion live for MZ
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-rollout/07-RESEARCH.md
@.planning/phases/07-rollout/07-01-SUMMARY.md
@.planning/phases/07-rollout/07-02-SUMMARY.md
@.planning/phases/06-production-hardening/06-USER-SETUP.md

# The script Plan 07-01 ships, that this plan runs:
@apps/web/scripts/provision-users.ts

# The cutover ceremony Plan 07-02 ships, that this plan executes for Mozambique:
@docs/CUTOVER.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Resolve eight open questions blocking the pilot cutover</decision>
  <context>
    Eight questions from `07-RESEARCH.md` need explicit answers from William before any user is provisioned. Each answer affects downstream choices: which CSV rows exist, which role each row has, which ingestion path the form-side flips to.

    The recommended path on each is in the option marked **(recommended)** below — an "approve all defaults" answer is valid and unblocks the cutover immediately.
  </context>
  <options>
    <option id="approve-all-defaults">
      <name>Approve all eight defaults (recommended)</name>
      <pros>
        - Q1 Group Sales role: hq_admin (read everything, no queue pollution)
        - Q2 Martin Cox: pure hq_admin (covers both reads)
        - Q3 Anele Dlamini (SZ admin): country_admin role, queue access via separate seat if needed later
        - Q4 Joyce Gachuhi (KE admin): country_admin
        - Q5 Other 10 active countries: contact lists collected per-country during 07-04 rollout, not all at once now
        - Q6 Pilot ingestion path for MZ: Path 2 (n8n bridge) — already exists, lower-risk than asking Paratus IT for direct webhook config on day one
        - Q7 HQ org transfer recipient: William as Supabase Owner, William + IT lead as Vercel Members
        - Q8 Loom hosting: para.group.n8n@gmail.com (master account — links survive eventual full agency-out)
        Unblocks the cutover with one decision. Every choice has the strongest documented rationale in 07-RESEARCH.md.
      </pros>
      <cons>
        - If William wants Group Sales to actually have queue access in MZ, we need Q1 to flip to per-country agent provisioning (would touch the CSV row shape).
        - Path 2 (n8n) introduces a hop; latency target stays well under 30 s but is technically slower than Path 1 direct webhook.
      </cons>
    </option>
    <option id="custom-answers">
      <name>Provide custom answers per question</name>
      <pros>
        - Lets William diverge from defaults where the operational reality at Paratus differs from the research assumption.
        - Captures any per-country variance (e.g. KE wants Path 1 but MZ wants Path 2) before it becomes a rollout discovery.
      </pros>
      <cons>
        - Slower — needs William to write out each answer in `07-USER-SETUP.md` before the cutover continues.
      </cons>
    </option>
  </options>
  <resume-signal>Reply "approve all defaults" or paste answers in the format `Q1: <answer>`, `Q2: <answer>`, ... — Claude appends them to `.planning/phases/07-rollout/07-USER-SETUP.md`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build rollout-contacts.csv + Resend-SMTP wiring + Mozambique provisioning</name>
  <files>.planning/rollout-contacts.csv, .gitignore, .planning/phases/07-rollout/07-USER-SETUP.md</files>
  <action>
**2a. Update `.gitignore`** with a precise entry to keep the real CSV out of git:
```
.planning/rollout-contacts.csv
!.planning/rollout-contacts.csv.example
```
(The `!` negation preserves the example file Plan 07-01 ships.)

**2b. Build `.planning/phases/07-rollout/07-USER-SETUP.md`** mirroring the Phase 6 user-setup format. Sections:
1. **Open question answers from William** — record verbatim what William resolved in Task 1's checkpoint, with date.
2. **Supabase Auth SMTP wiring** — checklist of dashboard steps (the `dashboard_config` from this plan's frontmatter `user_setup`). Include a "verification" sub-step: trigger a `supabase.auth.resetPasswordForEmail` for Gerhard's own test account post-config; assert the email lands inbox-not-spam, lands within 30 s, and the from-address matches `SLA_ALERT_FROM_EMAIL`.
3. **GitHub paratusgroup org creation** — same posture, dashboard checklist + verification (`gh org list` shows the org once `gh auth refresh -s admin:org` is run).
4. **Vercel production env vars carry-over from Phase 6** — copy the table of 13 missing env vars from `06-USER-SETUP.md` and gate the cutover on them being checked. Do NOT duplicate the full descriptions — link back to 06-USER-SETUP for source. List only the var names + a single "verified-in-Vercel" checkbox per var.
5. **Pilot decisions log** — pilot country (MZ — confirmed by William), ingestion path for pilot (Path 1 or Path 2 per Q6), expected first-lead-time once webhook flips.

**2c. Build `.planning/rollout-contacts.csv`** with Mozambique rows confirmed by William's answers PLUS staged rows for the other 11 active countries (Plan 07-04 will activate those rows by running the script with `--country=<CC>` per country).

The CSV shape is locked by Plan 07-01: `email,role,country_code,full_name`. HQ admins (Group Sales per Q1's default + dedicated HQ admins per Q7) have empty `country_code`. Format and validation are tested by Plan 07-01's vitest suite.

If William's answer to Q1 was the default (Group Sales = hq_admin), include rows for Martin Cox / Thas Pillay / Stephen Petersen as `hq_admin` with empty country_code. If William diverged, capture per-country agent rows instead.

**2d. Verify SMTP + GitHub-org setup are live** — these are checklist items in the user-setup doc, but the cutover cannot proceed until they're done. Claude waits for the user to confirm both are complete (verification step in user-setup), then proceeds to step 2e.

**2e. Run the provisioning script for Mozambique only:**
```bash
# From the developer machine with .env.local containing SUPABASE_SERVICE_ROLE_KEY,
# NEXT_PUBLIC_SUPABASE_URL, RESEND_API_KEY, NEXT_PUBLIC_APP_URL=https://paratus-group-dashboards.vercel.app:
npx tsx apps/web/scripts/provision-users.ts --country=MZ .planning/rollout-contacts.csv
```
Expected output: one `event: 'user_provisioned'` line per Mozambique row, one `event: 'provision_summary'` summary line with `failed: 0`. If any row fails, the script exits 1 — pause the cutover, debug, do NOT continue to step 2f until every Mozambique row is provisioned.

**2f. Update `docs/CUTOVER.md` Mozambique section:**
Tick the first four checklist items (contact list confirmed, CSV updated, redirect URL added — verified in step 2b sub-2, provisioning script ran cleanly). Leave the rest unchecked — Task 3 walks through them.

AVOID:
  - Provisioning ANY country other than MZ in this plan. Plan 07-04 handles the other eleven once the pilot soak passes.
  - Skipping the SMTP wiring step on the pretext that the provisioning script doesn't need it (the script sends invites via Resend directly). It IS needed for password resets, email change, and any future magic-link flows — these all route through Supabase's own SMTP, not Resend. Without custom SMTP, the rollout's first password-reset traffic 429s after two attempts.
  - Committing `.planning/rollout-contacts.csv` (real PII). The git ignore entry is the contract; verify it's working with `git check-ignore -v .planning/rollout-contacts.csv` before any provisioning happens.
  - Provisioning across the cutover gate. The provisioning step happens AFTER William's open-question answers are in `07-USER-SETUP.md` and the SMTP/org setup are confirmed.
  </action>
  <verify>
- `git check-ignore -v .planning/rollout-contacts.csv` returns the gitignore line (not a blank line) — proves the real CSV won't be committed
- `cat .planning/phases/07-rollout/07-USER-SETUP.md` shows William's answers + verification checkboxes
- `npx tsx apps/web/scripts/provision-users.ts --dry-run --country=MZ .planning/rollout-contacts.csv` (dry-run repeat) parses cleanly with `failed: 0` projected
- After live run: query Supabase `auth.users` for `email LIKE '%@paratus.co.mz'` (or whichever Paratus domain MZ uses) — count matches the MZ row count in the CSV
- `select count(*) from user_roles where country_code='MZ'` matches the count of MZ rows with `role IN ('country_admin','agent')`
- Each provisioned user received the welcome email (verify via Resend dashboard `Logs` tab — every row shows `delivered: true`)
  </verify>
  <done>
- `.gitignore` updated, real CSV gitignored, example file preserved
- `07-USER-SETUP.md` captured William's answers + every external-setup checklist
- Resend-as-Auth-SMTP wired and password-reset test email landed
- `paratusgroup` GitHub org exists
- All 13 Phase 6 carry-over env vars verified in Vercel production
- Mozambique users provisioned, structured logs prove success, Resend logs prove delivery
- CUTOVER.md MZ section reflects the work done so far (first four checkboxes ticked)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mozambique pilot is provisioned. Path forward: smoke test → flip ingestion → start soak. The next checkpoint after this one lands in Plan 07-04 (soak validation).</what-built>
  <how-to-verify>
**Step 1 — Agent seat smoke test (≤ 5 min):**
1. Open the welcome email sent to the Mozambique agent test address (real Paratus agent's inbox; if William prefers we use a `+test` Gmail alias for first-touch, swap that in).
2. Click "Set up your account". Assert: lands on `/auth/accept-invite`, password setup form renders.
3. Set password (use a strong throwaway — record in 1Password under William's vault entry for handover). Submit.
4. Assert: redirected to `/<mz-slug>/queue` (Mozambique slug from `countries.slug`).
5. Assert: queue UI renders. To Call tab is empty (no leads ingested yet) or shows seeded leads if any. Header shows agent's full name.
6. Click the bottom-of-sidebar `?` help link. Assert: opens `docs/onboarding/agent.md` on GitHub in a new tab.

**Step 2 — Country admin seat smoke test (≤ 5 min):**
Same flow with the country admin email. Assert: lands on `/<mz-slug>` overview, KPI tiles render zeros, agents leaderboard shows the agent(s) we provisioned, audit log at `/<mz-slug>/audit` is empty (no writes yet).

**Step 3 — Cross-tenant smoke test (≤ 2 min):**
From the country admin's seat, attempt to open `/<other-country-slug>` (e.g. `/kenya` if KE exists in the slug map; substitute any active country). Assert: 403 / unauthorized / route-protect bounces them. Cross-country read MUST fail.

**Step 4 — Flip the form-side ingestion path:**
Per Q6 (Path 1 or Path 2). For Path 2 (default): activate the n8n workflow that proxies the Paratus form webhook to `https://paratus-group-dashboards.vercel.app/api/leads/ingest` with the production HMAC secret. For Path 1: ask William to update the Paratus form's webhook URL + HMAC config in the Paratus admin.

**Step 5 — First real lead:**
Wait up to 30 min for organic submission, OR submit a test lead through the actual paratus.africa form for Mozambique. Assert: lead appears in the agent's `My Leads → To Call` tab via realtime broadcast (no refresh needed). Lead shows the right country, right service, contact captured.

**Step 6 — Tick the cutover checklist boxes for what's done so far:**
Open `docs/CUTOVER.md` Mozambique section, check items 5–8 (the smoke-test items + first-lead-observed). Leave items 9-11 (24h soak metrics + audit-log review + William sign-off) for Plan 07-04 — those land at T+24h.

If anything fails, describe the issue in detail. Common failure modes: redirect URL not whitelisted (back to Task 2 step 2b sub-2), SMTP not yet propagated (wait 5 min, retry), JWT-hook returned null claims (verify `user_roles` row exists for the agent — if not, the script's ordering broke; this would be a Plan 07-01 regression).
  </how-to-verify>
  <resume-signal>Type "MZ pilot live, soak started" if all six steps pass; describe issues otherwise. The 24-48 h soak validation lands in Plan 07-04 — do not progress to the next country until then.</resume-signal>
</task>

</tasks>

<verification>
Plan 07-03 is complete when:
- [ ] William's eight answers captured in `07-USER-SETUP.md`
- [ ] Resend wired as Supabase Auth SMTP, password-reset test email delivered
- [ ] Accept-invite redirect URL whitelisted in Supabase Auth
- [ ] `paratusgroup` GitHub org exists
- [ ] All 13 Phase 6 carry-over env vars verified in Vercel production
- [ ] `.planning/rollout-contacts.csv` exists locally (gitignored), populated for all 12 countries
- [ ] Mozambique rows provisioned via `provision-users.ts --country=MZ` with zero failures
- [ ] Two seats (agent + country admin) successfully accepted invites and reached their dashboards
- [ ] Cross-country read attempt blocked
- [ ] Form-side ingestion flipped to production for Mozambique
- [ ] First real lead observed in production
- [ ] CUTOVER.md MZ section reflects the work done (boxes 1–8 ticked)
</verification>

<success_criteria>
- Pilot cutover live, Mozambique agents + admin can use the production system
- William has signed off the open questions and the SMTP/org setup
- Form-side ingestion live with production HMAC for MZ
- First real production lead observed end-to-end (form → ingest → assigned to agent → realtime arrival in queue)
- Plan 07-04 has clean preconditions to start the 24-48 h soak validation + roll out the remaining 11 countries
</success_criteria>

<output>
After completion, create `.planning/phases/07-rollout/07-03-SUMMARY.md` documenting:
- William's eight answers + the date received
- The exact SMTP config used (host, port, from-address) — for handover-doc continuity
- The MZ provision summary (rows in / users out / Resend delivery rate / time elapsed)
- The first-lead observation (timestamp / contact / agent assigned / time-to-realtime arrival)
- Any issues surfaced during smoke test + how resolved
- The exact preconditions Plan 07-04 inherits — especially the soak-start time so the 24h window is measurable
</output>
