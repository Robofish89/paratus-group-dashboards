# Session Handoff — 2026-04-27

This file is the explicit "you are here" snapshot. A future Claude Code session reading this should be able to resume cold without re-asking anything covered in `CLAUDE.md`, `.planning/PROJECT.md`, `PRD/`, or memory.

## Where we are

- Repository: `~/Projects/paratus-group-dashboards/` — initialised, committed twice, pushed to https://github.com/Robofish89/paratus-group-dashboards (private).
- Phase 0 (foundation scaffolding) is **DONE**.
- Phase 1 (Foundation: Next.js app scaffolds, auth, role routing, Vercel deploy) is **NOT STARTED**.

## Locked facts (do not re-ask)

| Fact | Value |
|---|---|
| Project type | Standalone product for **Paratus Group**. Not Paratus Namibia, not AMA / AMA Care |
| Master Google account | `para.group.n8n@gmail.com` |
| Supabase | Created under master account. Keys not yet captured into the repo |
| Vercel | Created under master account. Vercel plugin installed via `npx plugins add vercel/vercel-plugin` (loads on Claude Code restart) |
| GitHub | User's personal account `Robofish89`. Will transfer to a `para.group.n8n@gmail.com`-owned account/org at handover (runbook in `CREDENTIALS.md`) |
| Domain | Pending — subdomain on `paratus.africa` preferred, temp Vercel domain as fallback |
| Pricing | R30,000 fixed-price build + R2,500/month retainer |
| Active countries (v1) | Angola, Botswana, DRC, Eswatini, Kenya, Mozambique, Namibia, Rwanda, South Africa, Tanzania, Uganda, Zambia (12) |
| Coming-soon countries | Lesotho, Malawi, Zimbabwe (3) — seeded with `status='coming_soon'` |
| Architecture | Single Next.js 16 app under `apps/web` with role-grouped routes; single Supabase project; RLS multi-tenancy by `country_code` |
| Design system | Mirrored from AMA at `~/Projects/ama-amacare-stats-callback-dashboard` for brand congruence (visual only — no shared data/auth/infra) |
| Quality bar | "Boil the Ocean" — see `CLAUDE.md` § "The Standard" |
| Pilot country | Mozambique (default; pending William's confirm) |

## What credentials still need wiring (and when)

Captured in `CREDENTIALS.md` § "When credentials get wired". Summary:

- **Phase 1 scaffolding moment** (when `apps/web` exists):
  - User pastes Supabase URL / anon key / service-role key into `apps/web/.env.local` directly (not through chat — exposure risk discussed and confirmed)
  - Vercel env vars set via `vercel env add` (or the Vercel plugin's UI) for dev / preview / production
  - `vercel link` from `apps/web` against the project under `para.group.n8n@gmail.com`
- **Phase 6 (Production Hardening):** Resend (or similar) for SLA breach alerts
- **Phase 7 (Rollout):** domain DNS, SSL, final env var values

## What was committed

- `5dd372d` — `feat: initialize paratus-group-dashboards build with PRD, GSD scaffold, and AMA design system mirror` (72 files, 5,441 insertions)
- `214af88` — `docs: record GitHub repo URL and handover plan in CREDENTIALS`

## Documents written this session

Filled and committed:
- `CLAUDE.md` — project rules, brand, pricing, key decisions, Boil-the-Ocean standard
- `STYLE_GUIDE.md` — design tokens + components from AMA
- `CREDENTIALS.md` — service ownership matrix, env var list, GitHub handover runbook
- `SECURITY_CHECKLIST.md` — RLS + deployment hard gates
- `README.md` — project intro, structure, getting started
- `PRD/README.md` + 8 PRD docs: `overview.md`, `features.md`, `user-flows.md`, `data-model.md`, `lead-ingestion.md`, `technical.md`, `milestones.md`, `open-questions.md`
- `.planning/PROJECT.md`, `.planning/roadmap.md`, `.planning/config.json`

Memory files added (auto-load via `MEMORY.md`):
- `feedback_visual_companion.md` (pre-existing — visual fidelity)
- `feedback_dedicated_google_account_per_client.md`
- `feedback_boil_the_ocean_standard.md`
- `project_paratus_group_distinction.md`
- `project_paratus_group_master_account.md`

## Next action — exactly what to do when work resumes

1. **Verify Vercel plugin loaded** — after Claude Code restart, the plugin's 25 skills, 6 commands, 3 agents, hooks, and MCP should be available. Quick check: `/help` or look for vercel-* commands.
2. **Run** `/gsd:plan-phase 1` — produces `.planning/phases/01-foundation/PLAN.md`
3. **Execute Phase 1** to the Boil-the-Ocean bar:
   - Scaffold `apps/web` (Next.js 16 + App Router + TS) with shared package wiring (`@repo/ui`, `@repo/supabase`, `@repo/config`)
   - `apps/web/app/globals.css` imports `@repo/ui/theme.css`
   - Login page using `AuthLayout`, Supabase Auth wired
   - `middleware.ts` for auth + role/country routing
   - Three placeholder pages: `(hq)`, `(country-admin)/[country]`, `(sales-rep)/[country]/queue` rendering `DashboardLayout` with the Paratus sidebar
   - Logo + favicon wired into `apps/web/public/`
   - Migration `00001_rbac_schema.sql` adapted from AMA: role enum extended to `hq_admin | country_admin | agent`; `user_roles` table includes `country_code`; JWT custom hook injects `user_role` + `country_code` into claims
   - Supabase Custom Access Token Hook enabled in dashboard
   - Vercel project linked, env vars set, deploy from `main` working
   - All 8 phase-gate items in `.planning/roadmap.md` ticked: code shipped, tests green (`type-check` + `lint` + `build`), docs updated, demo runnable, security checklist re-run, visual fidelity check, commit + tag `phase-1-complete`, no dangling threads

## Open holds (non-blocking)

- Domain decision (with William)
- Pilot country confirm (with William; default Mozambique)
- 15 questions in `PRD/open-questions.md` (with William over time; defaults documented)

## How to read this project cold

If you've never seen this project: start with `CLAUDE.md`, then `.planning/PROJECT.md`, then this handoff, then `PRD/README.md` for the deep dive. Memory files load automatically. Visual contracts are in `docs/design-reference/`.

---
*Written 2026-04-27 13:50 SAST. Supersede with a new handoff doc per session if needed.*
