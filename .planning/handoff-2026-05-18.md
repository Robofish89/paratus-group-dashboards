# Session handoff — 2026-05-18

**Outcome: form-side lead ingestion is LIVE on production.**

## What shipped

- WordPress **Code Snippets** PHP snippet hooking `elementor_pro/forms/new_record`
  (committed source: `docs/integrations/elementor-ingest.php`). Independent of
  every Elementor Action After Submit → zero changes to any Paratus form,
  provably non-destructive. Final design **supersedes** the handoff-2026-05-15
  plan (Webhook action + `request_args` filter) — abandoned because the forms
  already have a native Webhook action (itself broken; see OBS-1).
- Migration **00020** applied to prod Supabase: `HQ` countries row (pseudo-tenant
  for "Paratus Africa Group (Head Office)" leads).
- `PARATUS_INGEST_SECRET` rotated (old value was encrypted/unrecoverable);
  Vercel prod redeployed (`czkmmjse1`, aliased `dashboards.paratus.africa`).
- Permission added to `.claude/settings.local.json` for the supabase-paratusgroup
  MCP write tools.

## Verified

Two production tests landed correctly, then deleted:
- `/form-test` → `form_slug=general-contact`, `country_code=HQ`
- Starlink page → `form_slug=starlink`, `country_code=BW` (reused-form resolver)

Live on all **8 in-scope** form IDs. 3 forms left un-wired (questions doc Q1).

## Resolver (full spec: `.planning/elementor-form-inventory-2026-05-18.md`)

- `form_slug` by (form_id, page-URL keyword) — `cda1b37`/`2c5fd13`/`ec2bdd9`
  each reused for 3 services.
- `country_code` from the country dropdown value (stores a *recipient email*;
  country encoded, e.g. `info.bw@paratus.africa`→BW), else page path, else `HQ`.

## Open / next

- `.planning/questions-for-paratus-group.md`: Q1 (3 unmapped forms), Q3 (who
  works HQ leads + form default), **OBS-1 (Paratus's own native Webhook action
  is broken pre-existing — flag to William/Shannon)**, Q2 (per-country user list).
- Task #7 remainder: provision HQ workers + RLS confirm + exclude HQ from
  country pickers — deferred, gated on Paratus Q3.
- Secret was echoed into the session transcript once; user accepted the local
  risk and declined a re-rotate.

## HQ drill-through (added end of session)

`hq_admin` viewing the HQ tenant in the UI:
- `799c147` — ingestion + 00020 (HQ countries row).
- `94de7d1` — added `hq` to `ACTIVE_COUNTRIES` so the `[country]` route guard
  stops 404'ing `/hq` (only hq_admin can reach it).
- `96cf42a` + migration **00021** — added `HQ` to the `public.country_code`
  ENUM (user_roles is enum-typed; `getCountryAgents('HQ')` was throwing an
  enum-cast 500 on `/hq/leads`). Applied to prod; DB-side, no redeploy needed.

**Verified working:** hq_admin (`para.group.n8n+hq@gmail.com`) → HQ overview row
+ `/hq/leads` list renders the live lead. 0 agents shown (none provisioned for
HQ yet — expected).

## What to focus on next

1. **Send the William/Shannon message** (drafted in chat) — covers Q1 (3
   unmapped forms), Q3 (who works HQ leads + form default), OBS-1 (their own
   native webhook is broken), Q2 (per-country user list). All in
   `questions-for-paratus-group.md`.
2. **Task #7 remainder (gated on Q3 answer):** provision HQ worker(s)
   (hq_admin sees them; need an assignable agent/admin for HQ so leads don't
   sit unassigned), confirm RLS for the HQ tenant end-to-end, decide whether
   `HQ` should be hidden from country-only pickers / labelled distinctly.
3. **Q2 (William): per-country user list** — still the main blocker for
   handing dashboards to the country teams (`provision-users.ts`).
4. Optional hygiene: `PARATUS_INGEST_SECRET` was echoed once into the session
   transcript; user accepted the local risk. Re-rotate cleanly if desired.

## Repo state

Committed + pushed to `main`: `799c147` (ingestion + 00020 + docs),
`94de7d1` (ACTIVE_COUNTRIES hq), `96cf42a` (00021 enum). Prod migrations
00020 + 00021 applied. `.claude/settings.local.json` (supabase-paratusgroup
MCP write perms) is gitignored — local only. This handoff committed separately.
PII export + /tmp artifacts deleted; all test leads deleted from DB.
