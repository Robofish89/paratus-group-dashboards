# Handoff — end of 2026-04-28

> Read this first when picking up tomorrow.

## Where we landed

**Phase 1 (Foundation) is closed.** Tag `phase-1-complete` on `origin/main`, production live at <https://paratus-group-dashboards.vercel.app>, all docs updated, smoke test green for all three test users (HQ / country-admin / agent).

Working tree is clean. `main` is up to date with `origin/main`. No half-done work, no uncommitted files, no in-flight builds.

## Tiny dashboard chores still owed (non-blocking)

These don't block Phase 2 — knock them out whenever:

1. **Supabase Site URL** — flip from `http://localhost:3012` to `https://paratus-group-dashboards.vercel.app` (Authentication → URL Configuration → top "Site URL" block → Save changes). Affects password-reset / email-confirmation links only.
2. **Vercel Preview Deployment Protection** — enable Vercel Authentication for Preview only (Settings → Deployment Protection). Production stays public.

## Picked up during the session, parked for later

- **`.planning/rollout-contacts.md`** (uncommitted) — captures William's first batch of names for Eswatini + Kenya, plus the 10 missing countries and the open questions about Group Sales / HQ roles. Asked William to redo the breakdown more cleanly. Don't act on this until his next batch arrives.
- **No data model work yet** — Phase 2 is a clean start.

## Tomorrow's first move — pick one

### Option A: Just say "Phase 2" or "go"
I'll start the Phase 2 GSD flow:
1. `/gsd:research-phase 2` — research data model + ingestion patterns (Supabase RLS multi-tenant, HMAC webhook, round-robin assignment) before planning.
2. `/gsd:plan-phase 2` — write the execute plans (likely 3 plans: schema migrations, DAL + ingest endpoint, CSV importer + round-robin).
3. `/gsd:execute-phase 2` — wave-based parallel execution.

### Option B: Knock out Supabase Site URL + Preview Protection first
~5 min in dashboards. Then jump to Phase 2.

### Option C: Build a demo with seed data on Phase 1
You mentioned "let's just keep building with our demos until we have all of the names." If you mean putting fake leads into the existing placeholder pages so you can show William a moving demo without waiting for Phase 2's full ingest pipeline → say so and I'll scope a one-off seed script that drops mock leads + agents into Mozambique only.

## What's already true (don't re-verify)

- Vercel CLI logged in as `paragroupn8n-9569` on the `paratusgroup` team
- Project `paratus-group-dashboards` linked locally; `.vercel/project.json` gitignored
- 9 env vars set in Vercel (URL/anon/service × prod/preview/dev)
- GitHub repo `Robofish89/paratus-group-dashboards` connected to Vercel; pushes to `main` auto-deploy
- Supabase MCP `supabase-paratus` works — `get_project_url`, `get_publishable_keys`, etc.
- Three test users (`para.group.n8n+hq@gmail.com`, `+country-admin@gmail.com`, `+agent@gmail.com`) seeded with `user_roles` rows; JWT custom-claims hook live

## GSD status

Yes — still using GSD. Phase 1 plans live at `.planning/phases/01-foundation/`. Roadmap in `.planning/roadmap.md`. Phase 2 directory will be created when we kick off (`.planning/phases/02-data-model/` or similar). The `06-production-hardening` directory is just a forward stub — not active until Phase 6.

## Sleep well

Nothing to babysit overnight. Vercel will only deploy if you push (you won't). No cron, no scheduled jobs running yet.
