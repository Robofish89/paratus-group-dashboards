---
phase: 06-production-hardening
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/web/app/api/health/route.ts
  - apps/web/lib/perf-budget.ts
  - lighthouserc.json
  - .github/workflows/perf-budget.yml
  - docs/RUNBOOK.md
  - docs/BACKUP_RESTORE.md
  - .planning/PROJECT.md
files_modified_notes: "Plan 03 also runs `vercel env add` and external monitor configuration but those are CLI/dashboard side-effects, not file edits."
autonomous: false
domain: nextjs+supabase
user_setup:
  - service: betterstack-uptime
    why: "External synthetic monitoring on /api/health (PRD requirement: every 5 minutes)"
    env_vars: []
    account_setup:
      - url: "https://betterstack.com/uptime"
        skip_if: "Already have a Better Stack workspace under para.group.n8n@gmail.com"
    dashboard_config:
      - task: "Create HTTP monitor"
        location: "Better Stack → Uptime → New Monitor"
        details: "URL: https://<production-domain>/api/health, method: GET, interval: 5 min, expected status: 200, alerts: email para.group.n8n@gmail.com + William's email (William to provide). Pause until Plan 03 deploys."
---

<objective>
Operational readiness: synthetic health monitoring, performance budget enforcement, backup/restore runbook, and the 48-hour pilot verification gate that closes Phase 6.

Purpose: Phase 6's done condition is "pilot country runs 48h with real leads, no incidents". This plan supplies the monitoring eyes (so an incident is detectable) and the runbook (so a recoverable incident is recoverable), then ends with the human-time pilot verification.
Output: `/api/health` endpoint, Lighthouse CI on PRs and main, RUNBOOK.md + BACKUP_RESTORE.md, production env vars audited, pilot 48h verification signed off.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/roadmap.md
@.planning/phases/06-production-hardening/06-01-SUMMARY.md
@.planning/phases/06-production-hardening/06-02-SUMMARY.md
@PRD/technical.md
@PRD/milestones.md
@SECURITY_CHECKLIST.md
@CREDENTIALS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Health endpoint + performance budget CI</name>
  <files>apps/web/app/api/health/route.ts, apps/web/lib/perf-budget.ts, lighthouserc.json, .github/workflows/perf-budget.yml</files>
  <action>
    1. `apps/web/app/api/health/route.ts` — GET handler returning JSON `{ ok: true, time, supabase: 'ok'|'fail', latencies: { db_ms } }`. Run a cheap Postgres `select 1` via service role (server-only), measure latency, return 200 if <500ms, 503 if Supabase unreachable or >500ms. NO auth required (it's a liveness probe). Cache `Cache-Control: no-store`.
    2. `apps/web/lib/perf-budget.ts` — programmatic Lighthouse config object (LCP < 2.0s, TBT < 200ms, CLS < 0.1, queue page interactive < 1.5s). Exported so `lighthouserc.json` can import.
    3. `lighthouserc.json` — Lighthouse CI config that runs against three URLs (login, country-admin home, sales-rep queue) and asserts the budget. Uses `lhci autorun` with assertions matrix.
    4. `.github/workflows/perf-budget.yml` — GH Action: on PR + on push to main, build the app, start `next start` against a Vercel preview-style env, run `lhci autorun`, fail the workflow if budgets break. Use `LHCI_GITHUB_APP_TOKEN` from secrets if available (otherwise log results to PR via `lhci collect --upload`).

    AVOID: testing perf against `next dev` (Turbopack stats are not representative). Always build then start.
  </action>
  <verify>
    - `curl https://<preview>/api/health` returns 200 with `ok: true`
    - Pull `vercel logs --since=1h` and confirm health pings every 5 min once Better Stack is wired
    - `npx lhci autorun --config=./lighthouserc.json` runs locally; intentionally regress one route's bundle, see budget fail
    - GH Action shows pass on a clean PR
  </verify>
  <done>Liveness probe returns DB latency; Lighthouse CI gates PRs against the perf budget; Better Stack monitor pings prod every 5 min (post-deploy).</done>
</task>

<task type="auto">
  <name>Task 2: Production env audit + RUNBOOK + backup/restore docs</name>
  <files>docs/RUNBOOK.md, docs/BACKUP_RESTORE.md, .planning/PROJECT.md</files>
  <action>
    1. Audit Vercel production env vars via `vercel env ls production`. For each required var, confirm present and marked Sensitive: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `LEAD_INGEST_SHARED_SECRET`, `LEAD_INGEST_HMAC_SECRET`, `RESEND_API_KEY`, `SLA_ALERT_FROM_EMAIL`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `CRON_SECRET`. Use `vercel env add <NAME> production` for any missing — prompt for value via secure stdin. Do NOT log values.
    2. Confirm preview-deploy password protection is enabled (`vercel project ls`, then check Vercel dashboard settings — log a TODO if not directly readable from CLI). Confirm Supabase Allowed Hosts is restricted to the production domain via `supabase projects api-keys` and the Supabase dashboard auth → URL configuration.
    3. `docs/RUNBOOK.md` — operations runbook covering:
       - **On-call contacts** (DigimountAI primary, William as escalation, Paratus form admin for ingestion-side issues)
       - **Common alerts** with diagnostic + remediation steps:
         * Better Stack 503 on `/api/health`
         * SLA breach storm (>50 in an hour) → likely a webhook misroute or country mass-import
         * Upstash rate-limit floods (someone leaked the secret → rotate `LEAD_INGEST_SHARED_SECRET` and `LEAD_INGEST_HMAC_SECRET`)
         * Supabase auth provider down
       - **Secret rotation procedure** (per service: how to generate new value, where to update in Vercel, in n8n / Paratus form admin, in Supabase if applicable)
       - **Pilot incident triage cheat sheet** — first 5 minutes when something breaks during the 48h window
    4. `docs/BACKUP_RESTORE.md` — Supabase backup/restore procedure:
       - Confirm Supabase project tier supports point-in-time recovery (verify in dashboard); document the retention window
       - Manual backup procedure: `supabase db dump --schema public,auth -f backups/$(date +%F).sql`
       - Restore drill: scripted restore into a Supabase branch (`supabase branches create test-restore`), apply dump, verify a known lead row exists. Run this drill once before pilot launch and record the date in the doc.
       - RTO / RPO statement: best-effort 1h RTO, ≤24h RPO (whatever Supabase tier provides, recorded honestly).
    5. Update `.planning/PROJECT.md` — append a Phase 6 bullet under "Validated" once tasks 1+2 are green.

    AVOID: documenting an RTO/RPO tighter than what Supabase actually provides. Honest numbers > impressive numbers.
  </action>
  <verify>
    - `vercel env ls production` lists every required var, all marked Sensitive
    - Restore drill executes successfully in a Supabase branch and the runbook records the date
    - `docs/RUNBOOK.md` and `docs/BACKUP_RESTORE.md` reviewed line by line — no TBDs, no placeholders, every step is copy-pasteable
  </verify>
  <done>Every production env var present and Sensitive; preview password-protected; Supabase URLs locked to prod domain; restore drill complete and dated; RUNBOOK + BACKUP_RESTORE shippable to William.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 is structurally complete. Pilot country (locked in Plan 01) is now running on real form ingestion with rate-limited, HMAC-validated webhook (or n8n bridge), SLA breach emails, audit log, security headers, automated cross-country RLS test, health monitoring, and a documented runbook.

    What needs human eyes: the 48-hour pilot soak. Phase 6 done condition (per `.planning/roadmap.md` and `PRD/milestones.md`): "Pilot country runs live for 48h with real leads, no incidents, no cross-country leakage observed."
  </what-built>
  <how-to-verify>
    Start at T+0 (deploy to production):

    1. **Confirm pilot is receiving leads (T+0 to T+1h):**
       - Watch Vercel logs: `vercel logs --since=1h --follow` — expect `lead_ingest` structured events as Paratus forms submit.
       - Watch Better Stack uptime monitor — expect 100% over the first hour.
       - Open the country admin dashboard for the pilot country — expect new leads appearing in realtime.

    2. **Cross-country leakage spot-check (T+1h):**
       - Sign in as a country admin from a NON-pilot country.
       - Confirm the pilot country's leads do NOT appear anywhere in the dashboard.
       - Sign in as HQ, drill into pilot country, then drill back to overview — confirm cross-country drill works as designed.

    3. **SLA alert path (T+1h to T+24h):**
       - Wait for at least one organic SLA breach (or seed one in staging mirror).
       - Confirm email arrived at country admin's inbox within 60s of breach.
       - Confirm the same lead does NOT re-alert on subsequent cron runs.

    4. **Performance check (any time during 48h):**
       - Open queue page on a phone (real device, not emulator).
       - Confirm load < 1.5s after auth.
       - Confirm `/api/health` returns 200 with `db_ms < 200`.

    5. **48h close-out (T+48h):**
       - Pull Vercel logs for the full window. Count leads ingested, SLA breaches alerted, errors thrown.
       - Pull Better Stack incident log — expect zero unscheduled outages.
       - Pull Supabase logs — review for any RLS denial that shouldn't have happened.
       - Run `npm run test:rls` against production data — green.
       - Re-walk SECURITY_CHECKLIST.md — every box still ticked.

    Approve only if: zero cross-country leakage, zero unhandled errors, SLA emails arrive, Better Stack shows ≥99.9% uptime, performance budget met on the queue page on a real phone.
  </how-to-verify>
  <resume-signal>Type "approved — phase 6 done" once the 48h soak is clean. Describe any issues found and we'll triage before tagging `phase-6-complete`.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `/api/health` returns 200 in production with db_ms < 500
- [ ] Better Stack monitor green for ≥48h
- [ ] Lighthouse CI green on main
- [ ] All required env vars present in Vercel production, Sensitive flag set
- [ ] Restore drill performed and dated in `docs/BACKUP_RESTORE.md`
- [ ] User signed off on the 48h pilot soak
- [ ] `git tag phase-6-complete` ready to apply
</verification>

<success_criteria>
- Pilot country ran 48h with real leads, zero incidents, zero cross-country leakage
- All Phase 6 milestones from `PRD/milestones.md` ticked
- Phase gates from `.planning/roadmap.md` § "Phase Gates — Boil the Ocean Standard" all true
- `.planning/PROJECT.md` "Validated" updated with Phase 6 bullet
- No dangling threads: every five-minute tidy-up surfaced during this phase is closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-hardening/06-03-SUMMARY.md` with frontmatter (phase, subsystem: operations, requires: [06-01, 06-02], provides: [health-endpoint, perf-budget-ci, runbook, backup-procedure], affects: [07-rollout], tags: [observability, ops, compliance], key-decisions: [pilot-country-final], key-files: [list]) and sections: Accomplishments (incl. 48h pilot incident report), Issues Encountered, Deviations, Next Phase Readiness (gate to Phase 7 rollout).
</output>
