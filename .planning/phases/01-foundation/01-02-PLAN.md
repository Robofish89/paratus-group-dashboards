---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/supabase/migrations/00001_rbac_schema.sql
  - packages/supabase/src/types/index.ts
  - packages/supabase/src/types.ts
  - packages/supabase/src/dal/index.ts
  - packages/supabase/src/dal/users.ts
  - packages/supabase/src/schemas/index.ts
  - packages/supabase/src/schemas/auth.ts
  - apps/web/middleware.ts
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/login/actions.ts
  - apps/web/app/(auth)/unauthorized/page.tsx
  - apps/web/app/(auth)/auth/callback/route.ts
  - apps/web/app/api/auth/logout/route.ts
  - apps/web/app/page.tsx
  - apps/web/app/(hq)/layout.tsx
  - apps/web/app/(country-admin)/[country]/layout.tsx
  - apps/web/app/(sales-rep)/[country]/queue/layout.tsx
  - apps/web/app/_lib/auth.ts
  - apps/web/.env.local.example
  - SECURITY_CHECKLIST.md
autonomous: false
domain: nextjs+supabase
user_setup:
  - service: supabase
    why: "RBAC migration + Custom Access Token Hook must be applied to the live Paratus Group Supabase project, and three test users (one per role) must be seeded so the role-routing milestone is verifiable."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → Data API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API Keys → anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API Keys → service_role key (mark Sensitive in Vercel later)"
    account_setup:
      - url: "https://supabase.com/dashboard"
        skip_if: "Already signed into the Paratus Group Supabase project under para.group.n8n@gmail.com"
    dashboard_config:
      - task: "Enable Custom Access Token Hook"
        location: "Supabase Dashboard → Authentication → Hooks → Custom Access Token Hook"
        details: "After migration 00001 applies, set the hook to public.custom_access_token_hook (Postgres). Save and reload — without this, JWT claims will not contain user_role / country_code, and middleware will keep everyone on the unauthorized page."
      - task: "Create three test users"
        location: "Supabase Dashboard → Authentication → Users → Add user"
        details: |
          Create with email + password (any password — these are test accounts):
          1. hq-test@paratus.group  → assign user_roles row: role='hq_admin', country_code=NULL
          2. country-admin-test@paratus.group → role='country_admin', country_code='MZ'
          3. agent-test@paratus.group → role='agent', country_code='MZ'
          Use the SQL Editor: INSERT INTO public.user_roles (user_id, role, country_code, is_active) VALUES (...);
          The user UUIDs come from auth.users — copy from the Authentication → Users panel after creating each.
      - task: "Restrict Allowed Hosts"
        location: "Supabase Dashboard → Authentication → URL Configuration"
        details: "Site URL = http://localhost:3012 for now. Plan 03 will add the production Vercel URL."
    local_dev:
      - "Paste the three NEXT_PUBLIC_/SERVICE_ROLE values into apps/web/.env.local (gitignored). Do NOT paste them into chat."
      - "Run the migration locally if you have Supabase CLI: supabase db push --db-url \"$DATABASE_URL\" (or paste SQL into the dashboard SQL Editor)."
---

<objective>
Apply the RBAC migration, wire Supabase Auth + role/country middleware, and ship a working login → role-routing flow against the three placeholder pages from Plan 01.

Purpose: Phase 1's "done" criterion is that a user with each role can sign in and land on the correct placeholder. This plan delivers that end-to-end. It is the highest-risk plan in Phase 1 — the JWT custom claim hook + middleware is the foundation Phase 2's RLS + Phase 3's realtime channels both depend on.
Output: A login page, three working role-routes guarded by middleware, RBAC migration applied to the live Supabase project, JWT custom claim hook enabled, three seeded test users, and a green E2E verification: each test user lands on their expected placeholder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/roadmap.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
@PRD/technical.md
@PRD/data-model.md
@SECURITY_CHECKLIST.md
@CREDENTIALS.md
@packages/supabase/src/server.ts
@packages/supabase/src/client.ts
@packages/supabase/src/middleware.ts
@packages/supabase/src/admin.ts
@packages/ui/src/layouts/auth-layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 00001_rbac_schema.sql with country-aware claims and update Supabase package types</name>
  <files>packages/supabase/migrations/00001_rbac_schema.sql, packages/supabase/src/types/index.ts, packages/supabase/src/types.ts, packages/supabase/src/schemas/index.ts, packages/supabase/src/schemas/auth.ts, packages/supabase/src/dal/index.ts, packages/supabase/src/dal/users.ts</files>
  <action>
    Write the RBAC migration. The structure mirrors AMA's `~/Projects/ama-amacare-stats-callback-dashboard/packages/supabase/migrations/00001_rbac_schema.sql` but the role enum and the hook payload diverge — read AMA's file as a starting shape then adjust.

    1. Migration `packages/supabase/migrations/00001_rbac_schema.sql`:
       - `CREATE TYPE public.app_role AS ENUM ('hq_admin', 'country_admin', 'agent');` — three roles, no `viewer`. HQ has no `country_code` (NULL); country_admin and agent must have one.
       - `CREATE TYPE public.country_code AS ENUM ('AO','BW','CD','SZ','KE','MZ','NA','RW','ZA','TZ','UG','ZM','LS','MW','ZW');` — 12 active + 3 coming-soon (data model already supports them per PROJECT.md). Pure Postgres enum, ISO 3166-1 alpha-2.
       - `CREATE TABLE public.user_roles (id UUID PK, user_id UUID REFERENCES auth.users ON DELETE CASCADE UNIQUE NOT NULL, role public.app_role NOT NULL, country_code public.country_code NULL, is_active BOOLEAN NOT NULL DEFAULT true, created_at, updated_at)`
       - Constraint: `CHECK ((role = 'hq_admin' AND country_code IS NULL) OR (role IN ('country_admin','agent') AND country_code IS NOT NULL))` — enforces "HQ has no country, others must".
       - `ALTER TABLE public.user_roles ENABLE ROW LEVEL SECURITY;`
       - RLS policies (mirror AMA's "Users read own role", "HQ admins read all", "HQ admins manage all"):
         - SELECT: `(auth.jwt() ->> 'user_role') = 'hq_admin'` OR `user_id = auth.uid()`
         - ALL (insert/update/delete): `(auth.jwt() ->> 'user_role') = 'hq_admin'` (with-check identical)
       - `handle_updated_at()` trigger function + `set_updated_at` trigger on `user_roles` (same as AMA — copy verbatim).
       - `custom_access_token_hook(event jsonb) RETURNS jsonb` — extends AMA's version: SELECT `role, country_code, is_active` from `user_roles`, then `jsonb_set` `claims.user_role`, `claims.country_code` (NULL → JSON null), `claims.user_active`. STABLE LANGUAGE plpgsql.
       - `GRANT EXECUTE ON FUNCTION public.custom_access_token_hook TO supabase_auth_admin;`
       - `GRANT USAGE ON SCHEMA public TO supabase_auth_admin;`
       - `GRANT SELECT ON public.user_roles TO supabase_auth_admin;`
       - `REVOKE EXECUTE ON FUNCTION public.custom_access_token_hook FROM authenticated, anon, public;`
       - File header comment block: explains how to apply (Supabase CLI or dashboard SQL editor) AND the manual step required afterwards (enable the hook in the dashboard — pointing to the checkpoint in this plan).

    2. `packages/supabase/src/types.ts` — author or update to export TypeScript unions matching the enums:
       ```ts
       export type AppRole = 'hq_admin' | 'country_admin' | 'agent';
       export const ACTIVE_COUNTRY_CODES = ['AO','BW','CD','SZ','KE','MZ','NA','RW','ZA','TZ','UG','ZM'] as const;
       export const COMING_SOON_COUNTRY_CODES = ['LS','MW','ZW'] as const;
       export const ALL_COUNTRY_CODES = [...ACTIVE_COUNTRY_CODES, ...COMING_SOON_COUNTRY_CODES] as const;
       export type CountryCode = typeof ALL_COUNTRY_CODES[number];
       export interface UserClaims { user_role: AppRole | null; country_code: CountryCode | null; user_active: boolean; }
       ```
       Re-export from `packages/supabase/src/types/index.ts` (keep both paths working — `src/types.ts` is consumed by `middleware.ts` directly already).

    3. `packages/supabase/src/dal/users.ts` (`import 'server-only';`):
       - `getCurrentUserClaims(): Promise<UserClaims | null>` — uses `createClient()` from `./server`, decodes the access token JWT, returns the typed claims or null if unauthenticated.
       - `getUserRoleRow(userId: string)` — admin-client-only read of the `user_roles` row for one user. Used by login flow to resolve where to redirect when JWT hasn't refreshed yet.
       Re-export from `packages/supabase/src/dal/index.ts`.

    4. `packages/supabase/src/schemas/auth.ts` — Zod:
       - `loginSchema = z.object({ email: z.string().email(), password: z.string().min(8).max(72) })`
       Re-export from `packages/supabase/src/schemas/index.ts`.

    5. Update `SECURITY_CHECKLIST.md` § "Auth & RLS" to note: migration 00001 has been authored; the Custom Access Token Hook MUST be enabled in the Supabase dashboard for JWT claims to populate; cross-country leakage tests are deferred to Phase 2 (when there's data to leak).

    AVOID:
    - Do NOT use `auth.users.raw_user_meta_data` for role (CLAUDE.md security rule: never use `user_metadata` for authorization).
    - Do NOT hardcode the country list in middleware — single source of truth is `ACTIVE_COUNTRY_CODES`/`ALL_COUNTRY_CODES` exported from `@repo/supabase`.
  </action>
  <verify>
    - `npm run type-check` clean
    - `npm run lint` clean
    - `npm run build` succeeds
    - Migration file is valid SQL: `psql --no-psqlrc -f packages/supabase/migrations/00001_rbac_schema.sql -d <empty postgres>` runs without error (or skip this check and rely on the dashboard SQL editor reporting success at the checkpoint).
    - `import { ACTIVE_COUNTRY_CODES, type AppRole, type UserClaims } from '@repo/supabase'` resolves from `apps/web` (smoke test: temporarily import in `app/page.tsx` to confirm, then remove).
  </verify>
  <done>Migration 00001 authored, TypeScript types + Zod schemas + DAL helpers exported from `@repo/supabase`. Quality gates green. Migration is NOT applied to the live Supabase project yet — that's the next checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Apply migration 00001, enable the Custom Access Token Hook, and seed three test users in the Paratus Group Supabase project</action>
  <instructions>
    I (Claude) cannot reach the live Supabase project under `para.group.n8n@gmail.com` — its keys are not in chat (correctly) and there is no MCP wired for it yet. So this is the unavoidable manual step.

    Three things to do, in order, in the Supabase Dashboard:

    1. **Apply the migration**
       - Open `packages/supabase/migrations/00001_rbac_schema.sql`
       - Supabase Dashboard → SQL Editor → New query → paste the file → Run
       - Confirm: no error, `user_roles` table appears in the Table Editor, both enums (`app_role`, `country_code`) are listed.

    2. **Enable the Custom Access Token Hook**
       - Authentication → Hooks → Custom Access Token Hook
       - Enable; select function `public.custom_access_token_hook` (Postgres)
       - Save

    3. **Create three test users + role rows**
       - Authentication → Users → Add user (email + password). Use any throwaway passwords; record them somewhere safe for the verification step:
         - `hq-test@paratus.group`
         - `country-admin-test@paratus.group`
         - `agent-test@paratus.group`
       - SQL Editor → run (substitute the UUIDs from auth.users):
         ```sql
         insert into public.user_roles (user_id, role, country_code) values
           ('<uuid-of-hq-test>',           'hq_admin',       null),
           ('<uuid-of-country-admin-test>','country_admin', 'MZ'),
           ('<uuid-of-agent-test>',        'agent',         'MZ');
         ```

    4. **Capture the three env values** into `apps/web/.env.local` (gitignored — do NOT paste them into this chat):
       ```
       NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
       NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
       SUPABASE_SERVICE_ROLE_KEY=<service-role-key>
       ```
       URL + anon key from Project Settings → API Keys (or Data API). Service role key from the same panel — treat it like a password.

    5. **Sanity check** in SQL Editor:
       ```sql
       select user_id, role, country_code, is_active from public.user_roles;
       ```
       Should return three rows.
  </instructions>
  <verification>After resume, I (Claude) will: (a) read `apps/web/.env.local` to confirm the three keys are present (without printing values), (b) start `apps/web` dev server, (c) attempt `supabase.auth.signInWithPassword` for each test user via a one-shot script and decode the returned JWT to verify `user_role` and `country_code` claims are present (proves the hook is wired). Failure to find claims means the hook wasn't enabled in step 2 — return to dashboard.</verification>
  <resume-signal>Type "applied" when migration is run, hook is enabled, three users + role rows exist, and env vars are pasted into apps/web/.env.local. If any step fails, paste the error message and I'll diagnose.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build login page, role-aware middleware, and the three role-route layouts</name>
  <files>apps/web/middleware.ts, apps/web/app/(auth)/login/page.tsx, apps/web/app/(auth)/login/actions.ts, apps/web/app/(auth)/unauthorized/page.tsx, apps/web/app/(auth)/auth/callback/route.ts, apps/web/app/api/auth/logout/route.ts, apps/web/app/page.tsx, apps/web/app/(hq)/layout.tsx, apps/web/app/(country-admin)/[country]/layout.tsx, apps/web/app/(sales-rep)/[country]/queue/layout.tsx, apps/web/app/_lib/auth.ts, apps/web/.env.local.example</files>
  <action>
    Implement the auth surface end-to-end. The existing `packages/supabase/src/middleware.ts` is AMA's role-hierarchy version — Paratus needs role + country routing, not hierarchy, so write a new app-level `apps/web/middleware.ts` that uses `@repo/supabase/server` and consumes the JWT claims from Task 1.

    1. `apps/web/middleware.ts`:
       - `export const config = { matcher: ['/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp|ico)$).*)'] };`
       - Refresh the Supabase session each request (use `@supabase/ssr`'s `createServerClient` with the request/response cookie bridge — pattern from AMA's `packages/supabase/src/middleware.ts`).
       - Call `supabase.auth.getUser()` (NEVER trust `getSession()` cookies alone for the auth gate — `getUser()` re-validates).
       - Excluded paths (no auth): `/login`, `/unauthorized`, `/auth/callback`, `/api/health` (the latter is a placeholder for Phase 6).
       - For everything else:
         - Unauthenticated → redirect to `/login?redirectTo=<original-path>`
         - Authenticated, decode JWT (atob the access_token, JSON.parse the middle segment), read `user_role` and `country_code` claims:
           - `user_active === false` → redirect `/unauthorized`
           - `user_role == null` → redirect `/unauthorized` (no role row in DB)
           - `user_role === 'hq_admin'`:
             - Allow any path
             - If they hit `/` → rewrite to `/hq` (group `(hq)` route is at `/hq` because of route group naming — actually route groups don't add to URL; root `/` resolves to `(hq)/page.tsx` once we put the HQ index there. Choose: group `(hq)` keeps `/` as the HQ home OR explicit `/hq`. Decision: keep group naming as `(hq)` and place HQ index at `apps/web/app/(hq)/page.tsx` resolving to `/`; no rewrite needed for HQ.)
           - `user_role === 'country_admin'`:
             - Read `country_code` claim → lower-case → `cc`
             - If pathname is `/` or starts with `/hq` → redirect to `/${cc}`
             - If pathname matches `/^\/[a-z]{2}(?:\/|$)/` and the prefix !== cc → redirect to `/${cc}` (block other countries)
             - Otherwise allow
           - `user_role === 'agent'`:
             - Same country-prefix rules
             - If pathname is `/` or doesn't end in `/queue` → redirect to `/${cc}/queue`
       - Final return: `supabaseResponse` (the response carrying refreshed cookies).

    2. `apps/web/app/_lib/auth.ts`:
       - `requireUser()` — server helper used by route group layouts; calls `createClient()` from `@repo/supabase/server`, `getUser()`, throws/redirects to `/login` if missing.
       - `requireRole(roles: AppRole[])` — same + decodes claims, checks role membership, redirects `/unauthorized` if mismatched. Returns `{ user, claims }`.
       - `requireCountry(countryParam: string, claims: UserClaims)` — for country-scoped layouts: confirm the URL country matches the claim country (defense-in-depth — middleware already enforces, but Server Components must not trust path alone).

    3. `apps/web/app/(hq)/layout.tsx`:
       - Server Component layout that calls `requireRole(['hq_admin'])`. Children inherit.
       - Note: the `(hq)` group does not add a path segment; HQ users land on `/` which resolves to `apps/web/app/(hq)/page.tsx` (move the file from `apps/web/app/(hq)/page.tsx` if not at `/` already — the layout group structure means `(hq)/page.tsx` IS the `/` route). Update Plan 01's `apps/web/app/page.tsx` redirect: REMOVE it, because the route group resolves naturally.
       - **Edge case:** if HQ doesn't have an index, `/` 404s. Confirm `(hq)/page.tsx` exists (Plan 01 Task 2 created it) and the temporary `app/page.tsx` redirect is removed. Final structure:
         ```
         app/page.tsx                     ← DELETE (was Plan 01 redirect)
         app/(hq)/layout.tsx              ← NEW, requireRole(['hq_admin'])
         app/(hq)/page.tsx                ← from Plan 01, becomes the HQ index at /
         ```

    4. `apps/web/app/(country-admin)/[country]/layout.tsx`:
       - Calls `requireRole(['country_admin', 'hq_admin'])` — HQ admins can drill into country views read-only.
       - Calls `requireCountry(country, claims)` ONLY when role is `country_admin`. HQ bypasses (they can read any country).
       - Returns `{children}`.

    5. `apps/web/app/(sales-rep)/[country]/queue/layout.tsx`:
       - Calls `requireRole(['agent', 'hq_admin'])`. (Country admins do not access the agent queue directly.)
       - Country gate same as above.

    6. `apps/web/app/(auth)/login/page.tsx`:
       - Client Component (form) wrapping the Server Action.
       - Renders inside `AuthLayout` from `@repo/ui` with logo + tagline "Paratus Group Dashboards".
       - Email + password fields (from `@repo/ui` `Input`, `Label`, `Button`).
       - On submit → call `loginAction` (Server Action from `actions.ts`).
       - Display error from server action result.

    7. `apps/web/app/(auth)/login/actions.ts`:
       - `'use server'`
       - `loginAction(formData: FormData)`:
         - `loginSchema.safeParse({ email, password })` → on fail, return `{ error: 'invalid_input' }`
         - `supabase.auth.signInWithPassword(...)` → on fail, return `{ error: 'invalid_credentials' }` (do not echo Supabase's raw message — generic only)
         - On success, look up the user's role row via `getUserRoleRow(user.id)` (admin client) and compute redirect target:
           - `hq_admin` → `/`
           - `country_admin` → `/${country_code.toLowerCase()}`
           - `agent` → `/${country_code.toLowerCase()}/queue`
         - `redirect(target)`. If `searchParams.redirectTo` is present and starts with `/`, prefer it (light open-redirect protection).

    8. `apps/web/app/(auth)/unauthorized/page.tsx`:
       - Static page rendered in `AuthLayout`. "You don't have access to this dashboard. Contact your administrator." + `Sign out` button POSTing to `/api/auth/logout`.

    9. `apps/web/app/(auth)/auth/callback/route.ts`:
       - GET handler: handles Supabase email-link callbacks for future flows (password reset, magic link). For now, exchange `code` for session via `supabase.auth.exchangeCodeForSession`, redirect to `/`. Even if unused this phase, ship it — Phase 6 password recovery uses it.

    10. `apps/web/app/api/auth/logout/route.ts`:
        - POST handler. `supabase.auth.signOut()`. Redirect `/login`.

    11. Add a "Logout" link in `DashboardLayout`'s sidebar footer (small enhancement to `packages/ui/src/layouts/dashboard-layout.tsx` if it doesn't already accept an `onLogout` or a `logoutHref` prop — wire it to `/api/auth/logout` via a `<form method="POST" action="/api/auth/logout">`).

    12. Update `apps/web/.env.local.example` if it didn't already include the three keys (Plan 01 should have, but verify).

    AVOID:
    - Do NOT use `getSession()` for the auth gate in middleware — only `getUser()` is trustworthy (Supabase re-validates the access token).
    - Do NOT log JWT contents or full email addresses in any log line.
    - Do NOT redirect to user-supplied URLs without the `startsWith('/')` guard (open-redirect).
    - Do NOT hand-roll cookie reading — use `@supabase/ssr`'s cookie bridge per AMA's existing `packages/supabase/src/middleware.ts` shape.
  </action>
  <verify>
    - `npm run type-check` clean
    - `npm run lint` clean
    - `npm run build` succeeds
    - With dev server running and the three test users seeded:
      - `localhost:3012/` → not logged in → 302 to `/login?redirectTo=/`
      - Sign in as `hq-test@paratus.group` → land on `/` rendering HQ layout
      - Sign out → sign in as `country-admin-test@paratus.group` → land on `/mz` rendering country-admin layout
      - Same user attempts `/ke` → 302 to `/mz`
      - Sign out → sign in as `agent-test@paratus.group` → land on `/mz/queue`
      - Same user attempts `/mz` (no `/queue`) → 302 to `/mz/queue`
      - Logout button on each layout signs out and returns to `/login`
    - Browser DevTools: zero console errors, no `localStorage` writes related to auth (only httpOnly cookies)
  </verify>
  <done>Login → role/country routing → logout works for all three test users without a single console error or extra hop. JWT claims are decoded once in middleware and again in route layouts (defense-in-depth). Plan 01's temporary `app/page.tsx` redirect is removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1's role-routing milestone: login → role-aware redirect → branded placeholder. Three test users on the live Paratus Group Supabase project, JWT custom claim hook injecting `user_role` + `country_code`, middleware enforcing both on every request, layouts double-checking role + country before rendering.
  </what-built>
  <how-to-verify>
    Run `npm run dev` from the repo root. Then in a clean browser (or incognito) walk through ALL of these:

    1. Visit `http://localhost:3012/` while logged out → you land on `/login` with `redirectTo=/` in the URL.

    2. Sign in as `hq-test@paratus.group` (use the password you set in the dashboard checkpoint):
       - Lands on `/` showing HQ Overview placeholder, branded sidebar, accent-orange active nav row.
       - Visit `/mz` → renders the country-admin layout (HQ has read access).
       - Visit `/mz/queue` → renders the sales-rep layout.
       - Click logout in the sidebar → returns to `/login`.

    3. Sign in as `country-admin-test@paratus.group`:
       - Lands on `/mz` showing the country-admin layout.
       - Visit `/ke` → bounced back to `/mz` (cross-country block).
       - Visit `/` → bounced to `/mz`.
       - Logout works.

    4. Sign in as `agent-test@paratus.group`:
       - Lands on `/mz/queue`.
       - Visit `/mz` (no `/queue`) → bounced to `/mz/queue`.
       - Visit `/ke/queue` → bounced to `/mz/queue`.
       - Logout works.

    5. Visit `/atlantis` (unknown country) → 404 page.

    6. Browser DevTools (any of the above sessions):
       - Network tab: zero 500s.
       - Console tab: zero errors, zero warnings (one Next dev-mode hint about Turbopack is fine).
       - Application → Cookies: `sb-*-auth-token` present; httpOnly + Secure flags set (Secure flag may be missing on plain http localhost — that's expected; will be Secure in production).
       - Application → Local Storage: no auth-related keys.

    7. Side-by-side vs. `docs/design-reference/`:
       - Login screen close-enough to `AuthLayout`'s baked design (this is internal — minor drift OK).
       - Three placeholder pages match Plan 01's output (no regression from Plan 01's check).

    If any of the above fails, describe the failure (which user, which step, what you saw vs. expected). Otherwise, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" if every step passes, or describe the failure precisely.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check` clean
- [ ] `npm run lint` clean
- [ ] `npm run build` succeeds
- [ ] Migration 00001 applied to the live Paratus Group Supabase project
- [ ] Custom Access Token Hook enabled in the Supabase dashboard
- [ ] Three test users seeded with role + country rows
- [ ] All six role-routing scenarios from the human-verify checkpoint pass
- [ ] No `console.log`, no `any`, no TODOs, no half-wired UI
- [ ] `SECURITY_CHECKLIST.md` § "Auth & RLS" reflects the new state
</verification>

<success_criteria>
- Migration 00001 in `packages/supabase/migrations/` and applied to the live project
- JWT custom claim hook returns `user_role`, `country_code`, `user_active` in every access token
- Three test users (one per role) sign in and land on the correct placeholder route
- Cross-country access attempts are blocked at middleware
- Role-route layouts double-check claims as defense-in-depth
- Logout clears the session and returns to `/login`
- Quality gates green; no console errors anywhere
- Open-redirect protection on `redirectTo` query param
- No use of `user_metadata` for authorization (security rule)
- Service role key never appears in any `NEXT_PUBLIC_*` variable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with frontmatter (phase: 01-foundation, plan: 02, subsystem: auth, requires: [01-01], provides: [rbac-migration, auth-middleware, role-routing, login-flow], affects: [01-03, 02-*, all-future-phases], tags: [security, rls, auth, jwt-hook], key-decisions: [three-role-enum, country-code-enum-with-coming-soon, jwt-claim-hook-pattern, role-grouped-routes-no-path-prefix-for-hq], key-files: [packages/supabase/migrations/00001_rbac_schema.sql, apps/web/middleware.ts, apps/web/app/_lib/auth.ts, apps/web/app/(auth)/login/actions.ts]) and sections: Accomplishments, Issues Encountered, Deviations from Plan, Security Notes, Next Phase Readiness (must call out: Phase 2 RLS policies will use `(auth.jwt() ->> 'country_code')` as the tenant scope — this plan is the foundation that pattern depends on).
</output>
