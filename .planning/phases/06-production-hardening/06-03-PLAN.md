---
phase: 06-production-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/middleware.ts
  - apps/web/proxy.ts
  - apps/web/next.config.ts
  - apps/web/app/api/leads/ingest/route.ts
  - packages/supabase/migrations/00016_phase1_rls_initplan.sql
  - packages/supabase/migrations/00017_broadcast_function_lockdown.sql
  - packages/supabase/src/lib/rate-limit.ts
  - packages/supabase/src/server.ts
  - packages/supabase/src/dal/events.ts
  - packages/supabase/src/dal/leads.ts
autonomous: true
domain: nextjs+supabase
user_setup:
  - service: upstash-redis
    why: "Distributed rate limiter (HTTP transport, Fluid-Compute-friendly) for /api/leads/ingest and auth paths."
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console → Redis Database → REST API → URL (after `upstash redis create paratus-ratelimit --region eu-west-1`). Mark Sensitive in Vercel."
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console → Redis Database → REST API → Token. Mark Sensitive in Vercel."
    account_setup:
      - url: "https://console.upstash.com/login"
        skip_if: "Already authenticated via `upstash auth login` under para.group.n8n@gmail.com"
    dashboard_config:
      - task: "Create Redis database (or via CLI: `upstash redis create paratus-ratelimit --region eu-west-1`)"
        location: "Upstash Console → Create Database → Redis"
        details: "Region must match Vercel deployment region (Vercel team `paratusgroup` deploys to FRA1; eu-west-1 is closest). Free tier (10k commands/day) is enough for the pilot — at 60 req/min cap on ingest + 5 req/min cap on auth, we're nowhere near the cap."
---

<objective>
One-shot production hardening sweep: rename `middleware.ts` → `proxy.ts` per Next.js 16, sweep Phase 1 RLS policies for InitPlan caching + role narrowing, lock down broadcast trigger functions, add Upstash rate limiting on `/api/leads/ingest` and auth paths, and converge `createServiceRoleClient` → `createAdminClient`.

Purpose: Each item alone is a small win; together they close most of STATE.md's "Phase 6 carry-overs" list and bring the security posture up to the SECURITY_CHECKLIST.md bar before pilot launch. The deprecation warning, the missing rate limit, the unwrapped Phase 1 policies, and the dual client names have been carried for 5 phases — Phase 6 is the surgical close.
Output: `proxy.ts` running with rate-limit on auth + ingest paths; migrations 00016 (Phase 1 user_roles + Phase 5 hq_group_topic InitPlan-cached) + 00017 (REVOKE on three broadcast trigger functions); single client name `createAdminClient`; verified five security headers in `next.config.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-production-hardening/06-RESEARCH.md
@SECURITY_CHECKLIST.md
@CLAUDE.md
@apps/web/middleware.ts
@apps/web/next.config.ts
@apps/web/app/api/leads/ingest/route.ts
@packages/supabase/migrations/00001_rbac_schema.sql
@packages/supabase/migrations/00008_realtime_broadcast.sql
@packages/supabase/migrations/00013_hq_overview.sql
@packages/supabase/src/server.ts
@packages/supabase/src/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 16 middleware → proxy codemod + verify build</name>
  <files>apps/web/middleware.ts → apps/web/proxy.ts</files>
  <action>
    1. **Verify pre-state:** `apps/web/middleware.ts` exists; `apps/web/proxy.ts` does not. The middleware is currently node-compatible (no `export const runtime = 'edge'` — verify by reading the file). If runtime is edge, STOP and surface the conflict (the proxy convention is Node-only per Next.js 16 docs); none expected based on Phase 1 history.

    2. **Run the codemod from `apps/web`:**
       ```bash
       cd apps/web && npx @next/codemod@latest middleware-to-proxy .
       ```
       The codemod renames `middleware.ts` → `proxy.ts`, renames the `middleware` named export to `proxy`, and adjusts any `next.config` properties that referenced the old name.

    3. **Manually verify the diff** before committing:
       - `apps/web/proxy.ts` exists; `apps/web/middleware.ts` does not.
       - The default export (or named `proxy` export) signature still receives `NextRequest` and returns `NextResponse | undefined`.
       - The `config.matcher` array is intact (Phase 1's matcher excludes static assets + Next.js internals).
       - No reference to `middleware` remains in `apps/web/` outside of legacy comments — `git grep -n 'middleware' apps/web/` should only return doc/comment hits, not code.

    4. **Run the full build** to confirm zero deprecation warnings: `cd apps/web && npm run build`. The "deprecated `middleware` convention" warning that fires today must be gone. If the build emits new warnings, fix them before moving on (e.g. import paths the codemod missed).

    5. **Note for Task 3 + Plan 06-04:** any future reference to "the middleware" should now say "the proxy". Update `CLAUDE.md`'s "Architecture" section if it names `middleware.ts`. Update `.planning/STATE.md`'s carry-over list to mark this resolved.

    AVOID: running the codemod on the monorepo root (`.` from repo root) — it walks the whole tree and there's only one Next app. AVOID hand-editing the rename if the codemod gets it 95% right; either accept the codemod fully or revert and try again with `--print` to see what it'd do.
  </action>
  <verify>
    - `ls apps/web/proxy.ts` returns the file; `ls apps/web/middleware.ts` errors
    - `cd apps/web && npm run build` clean — no deprecation warning about `middleware`
    - `grep -rn 'middleware' apps/web/proxy.ts` only finds string literals or comments, not code-level references
    - Local dev: `npm run dev` boots and the matcher still triggers on `/login`, `/api/auth/*` (existing Phase 1 + 2 e2e specs pass)
  </verify>
  <done>`proxy.ts` is the canonical name; build is warning-free; existing matcher behaviour unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 00016 — Phase 1 RLS InitPlan caching sweep + Migration 00017 — Broadcast trigger function lockdown</name>
  <files>packages/supabase/migrations/00016_phase1_rls_initplan.sql, packages/supabase/migrations/00017_broadcast_function_lockdown.sql</files>
  <action>
    1. **Audit pre-state with the Supabase MCP** (read-only, before writing migrations):
       - `mcp__supabase-paratusgroup__execute_sql` with `SELECT polname, pg_get_expr(polqual, polrelid), polroles FROM pg_policy WHERE polrelid = 'public.user_roles'::regclass;` — confirm the three Phase 1 policies (`HQ admins read all user_roles`, `Users read own user_role`, `HQ admins manage user_roles` after 00012) use `auth.jwt()`/`auth.uid()` UNWRAPPED with `polroles = '{0}'` (i.e. `PUBLIC`, not `authenticated`).
       - `mcp__supabase-paratusgroup__execute_sql` with `SELECT polname, pg_get_expr(polqual, polrelid) FROM pg_policy WHERE polrelid = 'realtime.messages'::regclass;` — confirm Phase 5's `hq_group_topic` policy from 00013 is unwrapped (Phase 2's three are correctly wrapped per STATE.md decision).

    2. **Migration `packages/supabase/migrations/00016_phase1_rls_initplan.sql`** — apply via `mcp__supabase-paratusgroup__apply_migration`:
       - For each Phase 1 `user_roles` policy: `DROP POLICY` then `CREATE POLICY ... ON user_roles FOR <op> TO authenticated USING ((SELECT auth.<expr>) ...) [WITH CHECK ...];` — the two changes per policy are (a) wrap `auth.jwt()` / `auth.uid()` in `(SELECT ...)` for InitPlan caching, (b) add `TO authenticated` to short-circuit anon callers (Supabase docs cite up to 99.78% improvement on the role-narrowing alone).
       - Apply the same treatment to Phase 5's `hq_group_topic` policy on `realtime.messages` from 00013.
       - Migration header: list each policy renamed/replaced, the `(SELECT ...)` wrap rule, and a one-liner explaining "InitPlan caching causes Postgres to evaluate the volatile session function once per query rather than once per row" — same comment style Phase 2 used in 00006.
       - DO NOT touch policies that already wrap (`leads`, `lead_events`, `callbacks`, all Phase 4 + 5 country/group views) — they were correct as shipped. The MCP audit in step 1 is the proof.
       - **Mind RESEARCH pitfall 3:** initPlan caching only helps for expressions independent of row data. All three target policies are pure session expressions — safe.

    3. **Migration `packages/supabase/migrations/00017_broadcast_function_lockdown.sql`** — apply via `mcp__supabase-paratusgroup__apply_migration`:
       - `REVOKE EXECUTE ON FUNCTION public.broadcast_lead_to_agent()   FROM PUBLIC, anon, authenticated;`
       - `REVOKE EXECUTE ON FUNCTION public.broadcast_lead_to_country() FROM PUBLIC, anon, authenticated;`
       - `REVOKE EXECUTE ON FUNCTION public.broadcast_lead_to_group()   FROM PUBLIC, anon, authenticated;`
       - These are SECURITY DEFINER trigger functions called by ROW-level triggers; trigger context runs as the table owner, so explicit GRANT to `authenticated` is unnecessary. Phase 2 plan 02-03 forgot to add the REVOKE; Phase 5 inherited the gap. This migration closes it.
       - Migration header: explain the trigger-context exception (no GRANT needed back) and reference the 00008 + 00013 migrations that defined these functions.

    4. **Regenerate types** after both migrations apply (no row-shape changes expected, but keep `database.types.ts` honest): `npx supabase gen types typescript --project-id tgswsdfaszvztbpczfve > packages/supabase/src/types/database.types.ts`.

    5. **Run advisors:** `mcp__supabase-paratusgroup__get_advisors --type security` and `--type performance`. Expected: at least one performance finding cleared (the `auth_rls_initplan` advisor on user_roles). If new findings surface, document in the SUMMARY rather than chasing them outside Phase 6's scope.

    AVOID: rewriting policies that are already correct (Phase 2 + 4 + 5 mostly are — only 00001's user_roles + 00013's hq_group_topic are stragglers). AVOID combining the two migrations into one — distinct concerns, distinct revert paths if one breaks. AVOID granting EXECUTE back on the broadcast functions — they don't need it.
  </action>
  <verify>
    - `mcp__supabase-paratusgroup__list_migrations` shows both 00016 and 00017 applied
    - `mcp__supabase-paratusgroup__execute_sql` re-running the audit query from step 1 shows all `user_roles` policies now wrapped + `polroles` no longer includes 0 (PUBLIC)
    - `mcp__supabase-paratusgroup__execute_sql` with `SELECT proname, has_function_privilege('authenticated', oid, 'EXECUTE') FROM pg_proc WHERE proname LIKE 'broadcast_lead_to_%';` returns `false` for all three
    - `mcp__supabase-paratusgroup__get_advisors --type performance` shows the `auth_rls_initplan` advisor cleared on `user_roles`
    - All existing integration tests (`apps/web/tests/*.test.ts`) still pass — RLS behaviour MUST be identical, just faster
  </verify>
  <done>Phase 1's `user_roles` policies + Phase 5's `hq_group_topic` are now `TO authenticated USING ((SELECT ...))`; three broadcast trigger functions have EXECUTE revoked from `PUBLIC, anon, authenticated`; advisor warning cleared; existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Upstash rate-limit lib + apply on /api/leads/ingest + apply in proxy.ts on auth paths + createAdminClient convergence + verify security headers</name>
  <files>packages/supabase/src/lib/rate-limit.ts, apps/web/app/api/leads/ingest/route.ts, apps/web/proxy.ts, apps/web/next.config.ts, packages/supabase/src/server.ts, packages/supabase/src/dal/events.ts, packages/supabase/src/dal/leads.ts</files>
  <action>
    1. **Install Upstash deps in `apps/web`:** `cd apps/web && npm i @upstash/ratelimit @upstash/redis`. Confirm Phase 6 is the first introduction (search `package.json`).

    2. **Rate-limit lib at `packages/supabase/src/lib/rate-limit.ts`** (`import 'server-only'`):
       - Build a single Redis instance (`Redis.fromEnv()`) when `process.env.UPSTASH_REDIS_REST_URL` is set; fail-open in dev (no env), fail-CLOSED in prod (`if (process.env.NODE_ENV === 'production' && !redis) throw new Error('UPSTASH_REDIS_REST_URL required in production')`).
       - Export named limiters with `Ratelimit.slidingWindow`:
         * `authLimiter` — 5 req / 60s, prefix `paratus:auth`, analytics on
         * `ingestLimiter` — 60 req / 60s, prefix `paratus:ingest`, analytics on
       - When `redis` is null (dev fail-open), export shim limiters that always return `{ success: true, limit, remaining, reset: 0 }` so the call sites don't have to branch.
       - **Wrap each `await limiter.limit(key)` call site in `try/catch`** (per RESEARCH pitfall 5): on Upstash error, log `{ event: 'ratelimit_error', message }` and ALLOW the request (fail-open for rate limit is the right trade-off; auth wrong-bearer or wrong-secret is still rejected by the underlying check). Document the trade-off in 06-05's RUNBOOK.md.

    3. **Apply on `/api/leads/ingest` route** at `apps/web/app/api/leads/ingest/route.ts`:
       - At the very top of the POST handler, BEFORE secret/HMAC validation: `const key = sha256(process.env.PARATUS_INGEST_SECRET ?? 'unset'); const { success, limit, remaining, reset } = await ingestLimiter.limit(key);`. Returning rate-limit headers on 401 keeps the headers stable so a client probing the endpoint can't side-channel the secret state.
       - On `success === false`, return `new Response('Rate limited', { status: 429, headers: { 'X-RateLimit-Limit': String(limit), 'X-RateLimit-Remaining': '0', 'X-RateLimit-Reset': String(reset), 'Retry-After': String(Math.max(1, Math.ceil((reset - Date.now()) / 1000))) } })`.
       - Keep the existing structured log; add `rate_limit_remaining` to the payload.

    4. **Apply in `apps/web/proxy.ts`** for auth paths:
       - Detect auth-flow requests: `pathname === '/login' || pathname === '/auth/callback' || pathname === '/auth/reset' || (pathname.startsWith('/api/auth/') && req.method === 'POST')`.
       - Key the limiter on `req.headers.get('x-forwarded-for')?.split(',')[0]?.trim() ?? 'unknown'` (Vercel populates `x-forwarded-for` with the originating IP).
       - On `success === false`, return a `NextResponse` with status 429 and `Retry-After`, same shape as the ingest route. Otherwise continue to the existing proxy logic.
       - **Order matters:** rate-limit BEFORE the cookie-session refresh that Phase 1 set up — a flooded auth path shouldn't burn Supabase requests.

    5. **createAdminClient convergence:**
       - `packages/supabase/src/server.ts` — DELETE the `createServiceRoleClient` export. Keep the file (it still hosts `createClient()` for cookie sessions).
       - `packages/supabase/src/dal/events.ts` — replace `import { createServiceRoleClient } from '../server'` with `import { createAdminClient } from '../admin'`; rename call sites accordingly.
       - `packages/supabase/src/dal/leads.ts` — same swap.
       - Run `git grep -n createServiceRoleClient` after — must be zero hits.
       - Run `npm run type-check` — must be clean.

    6. **Verify five security headers** in `apps/web/next.config.ts`:
       - Read the file. Expected from Phase 1: `Content-Security-Policy`, `Strict-Transport-Security`, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`. If `Permissions-Policy` is missing, ADD it: `'camera=(), microphone=(), geolocation=()'`. Phase 1's CSP is acceptable as-is for v1; do NOT broaden it without a documented reason.
       - Smoke test: `npm run build && npx next start` (or `vercel deploy --prebuilt` if quicker), then `curl -I https://<preview-url>/` and confirm all six headers are present.
       - If headers are already complete, the diff for `next.config.ts` is zero; record "verified, no changes" in the SUMMARY.

    AVOID: in-memory rate limiting (resets per Vercel cold start = useless). AVOID applying ingestLimiter BEFORE the proxy.ts global ratelimit on the same path — keep them at distinct layers (proxy.ts handles auth paths; the ingest route handles its own). AVOID renaming `createAdminClient` to anything else for "consistency" — it's the older name with more call sites; convergence is one-way.
  </action>
  <verify>
    - `npm run type-check` clean across the monorepo
    - `npm run lint` clean
    - `npm run build` clean
    - Local smoke against `npm run dev`: `for i in {1..7}; do curl -i -X POST http://localhost:3012/login; done` — request 6+ returns 429 with `Retry-After`
    - Local smoke: `for i in {1..62}; do curl -i -X POST http://localhost:3012/api/leads/ingest -H 'X-Paratus-Secret: ...'; done` — request 61+ returns 429
    - `curl -I https://paratus-group-dashboards.vercel.app/` (production) shows: `content-security-policy`, `strict-transport-security`, `x-frame-options`, `x-content-type-options`, `referrer-policy`, `permissions-policy`
    - `git grep -n createServiceRoleClient` returns zero hits
    - All Phase 2/3/4/5 integration tests still pass — convergence and rate-limit must not regress behaviour
  </verify>
  <done>Upstash limiter active on auth + ingest with fail-open-on-error semantics; five required headers present (six with Permissions-Policy); single client name `createAdminClient` across the codebase; STATE.md carry-overs for headers + convergence + rate-limit closed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run type-check`, `npm run lint`, `npm run build` clean
- [ ] `apps/web/proxy.ts` exists; `apps/web/middleware.ts` does not
- [ ] Migrations 00016 + 00017 listed in Supabase migrations
- [ ] `mcp__supabase-paratusgroup__get_advisors --type performance` shows the `auth_rls_initplan` advisor cleared on `user_roles`
- [ ] `mcp__supabase-paratusgroup__execute_sql` confirms all three `broadcast_lead_to_*` functions have no EXECUTE for `authenticated`
- [ ] Six security headers (incl. Permissions-Policy) on `curl -I` against preview/production
- [ ] Rate-limit returns 429 at documented thresholds; `try/catch` on Upstash error logs structured event and allows
- [ ] `git grep createServiceRoleClient` returns zero hits
- [ ] All existing integration + e2e tests still pass
</verification>

<success_criteria>
- Next.js 16 deprecation warning gone
- Phase 1 user_roles + Phase 5 hq_group_topic policies use InitPlan caching + role narrowing
- Three broadcast trigger functions have explicit EXECUTE revocation
- Auth + ingest paths rate-limited via Upstash; fail-open on Upstash outage; fail-closed in prod when env missing
- Single Supabase admin client name across the codebase
- Six security headers on every response
- 06-USER-SETUP.md generated with Upstash setup checklist
</success_criteria>

<output>
After completion, create `.planning/phases/06-production-hardening/06-03-SUMMARY.md` with frontmatter:

```yaml
---
phase: 06-production-hardening
plan: 03
subsystem: hardening-sweep
requires: ["01-foundation", "02-data-model-ingestion", "05-hq-overview"]
provides: ["proxy", "rate-limit", "rls-initplan-cached", "broadcast-locked-down", "single-admin-client"]
affects: ["06-04", "06-05"]
tags: ["security", "performance", "infra-debt"]
key-decisions: ["fail-open-vs-fail-closed-on-upstash-error", "single-admin-client-name"]
key-files: ["apps/web/proxy.ts", "packages/supabase/migrations/00016_phase1_rls_initplan.sql", "packages/supabase/migrations/00017_broadcast_function_lockdown.sql", "packages/supabase/src/lib/rate-limit.ts"]
---
```

Sections: Accomplishments (incl. measured InitPlan caching improvement on user_roles via advisor before/after), Issues Encountered, Deviations, Next Phase Readiness.
</output>
