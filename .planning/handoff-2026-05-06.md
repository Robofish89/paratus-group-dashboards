# Session handoff — 2026-05-06

**Pause reason:** User stepping away (not feeling well). Resume any time.
**Working tree:** clean.
**Branch:** `main` at `49763e5` — local and `origin/main` in sync.

---

## What shipped today

Twelve commits, all on `main`, all deployed Ready in Vercel production.

### Pre-William's-meeting (UI polish + RLS scoping)

| Commit | Subject |
|--------|---------|
| `a954a3d` | chore(deploy): throttle SLA-check cron to daily for Hobby-plan deploys |
| `fa639cb` | feat(hq): replace stubs with real Countries directory + Service Mix table |
| `3999cbf` | feat(hq): empty-state status dot + real Settings + stale comment cleanup |
| `f8623c2` | feat(hq): surface Phase 6 audit log on Overview via Recent Activity panel |
| `245fc55` | fix(country-admin): scope leads list + export to URL country (HQ leak) |
| `2199c7d` | feat(country-admin): empty-state banner for zero-lead countries |
| `e3afaaf` | feat(country-admin): add per-country Recent Activity panel + extend DAL |
| `1d4b6a2` | feat(country-admin): real Settings page (was 404'ing) |

### Post-meeting (planning record + advisor sweep)

| Commit | Subject |
|--------|---------|
| `7103551` | docs(07-05): close plan — PLAN + SUMMARY + STATE update |
| `53cb4bc` | docs: phase tags 1-6 are on origin — clear stale "push pending" notes |
| `06abaee` | docs(07-06): spec Supabase advisor sweep — Tier 1 migration drafted |
| `49763e5` | feat(07-06): apply advisor sweep — migration 00019 + close plan |

---

## Phase 7 status

| Plan | State |
|------|-------|
| 07-01 | ✅ shipped |
| 07-02 | ✅ shipped |
| 07-03 | ⚠️ scaffold-only — **blocked on William's Q1–Q8 + synchronous cutover ceremony** |
| 07-04 | ⏳ blocked on 07-03 live cutover completing |
| 07-05 | ✅ shipped (today's polish) |
| 07-06 | ✅ shipped (advisor sweep migration 00019 applied today) |

`STATE.md` reflects all of this. `MEMORY.md` updated.

---

## What's left in Phase 7 (and the milestone)

**Blocked on William** (synchronous presence + Q1–Q8 answers):
1. **07-03 live cutover ceremony** — Resend SMTP wiring in Supabase Dashboard, GitHub `paratusgroup` org creation, run `provision-users.ts --country=MZ`, smoke-test, flip MZ form-side ingestion to production, start 24–48 h soak.
2. **07-04 multi-country rollout + handover** — validate the soak, record the three role Looms, roll out the other 11 active countries via the same provision-users cadence, transfer GitHub repo to `paratusgroup` org, tag `phase-7-complete`, ship milestone v1.

**You can do any time alone (no William, no Claude):**
- Supabase Dashboard → Authentication → Settings → Password security → enable **"Prevent the use of leaked passwords"**. Single toggle. Closes the last open Supabase advisor item (`auth_leaked_password_protection`).

**Deferred carry-overs** (not blockers):
- Tier 2 advisor: `multiple_permissive_policies` × 14 — own plan with cross-tenant test gate.
- Tier 5 advisor: `unused_index` × 5 — re-evaluate after 90 d of steady-state load.
- Pro-tier Vercel upgrade — restores the SLA cron from daily back to per-minute (currently throttled to `0 6 * * *` on the Hobby plan; restoration is a 1-line change in `apps/web/vercel.json`).
- Pro-tier Supabase upgrade — required for PITR (RPO < 24 h).

---

## Q1–Q8 message — ready to forward to William

Below is the message I drafted for William. Either send it as-is (e.g. paste into email or Slack) or massage the tone to your voice. Reply paths: he can answer Q1–Q8 async; the four-step cutover ceremony needs ~30 min synchronous.

```text
Subject: Paratus Group Dashboards — final answers needed before pilot cutover (8 questions, ~15 min)

Hi William,

Quick set of decisions we need locked before we run the Mozambique pilot
cutover. For each I've put my recommendation; reply "ok" to that, or override.

Q1 — Group Sales role assignment. Martin Cox / Thas Pillay / Stephen Petersen
— who's seeing the group sales view, and at what level?
Recommendation: hq_admin for all three (read-everything, no agent queue).
Reply with names + emails for each, plus any divergence.

Q2 — Martin Cox dual-role. Does Martin need both hq_admin and a separate
Group Sales seat?
Recommendation: Pure hq_admin covers both reads. One login, one inbox,
no Gmail+alias workaround. OK?

Q3 — Eswatini admin (Anele Dlamini). country_admin for SZ — confirmed?
Need: Anele's email address. Confirm the role.

Q4 — Kenya admin (Joyce Gachuhi). country_admin for KE — confirmed?
Need: Joyce's email address. Confirm the role.

Q5 — Other 10 active countries (Angola, Botswana, DRC, Mozambique, Namibia,
Rwanda, South Africa, Tanzania, Uganda, Zambia). Need per-country contact list.
Need for each country: name + email + role (country_admin or agent).
1 admin + ≥2 agents per country is the minimum. Mozambique is the pilot,
so that one first.

Q6 — Mozambique pilot ingestion path. Sheets/email forms route via n8n
bridge → our /api/leads/ingest endpoint with the production HMAC secret.
Recommendation: Lock that for the pilot — lower change cost than asking
Paratus IT to wire a direct webhook on day one. We can revisit per-country
later. OK?

Q7 — HQ org transfer recipients. Who gets which seat shape on Supabase /
Vercel / GitHub?
Recommendation: You as Supabase Owner. You + your Paratus IT lead as
Vercel Members and GitHub Members. The master Google account
(para.group.n8n@gmail.com) keeps primary ownership. Reply with the IT
lead's name + email.

Q8 — Loom training video hosting. Three onboarding videos (HQ Admin /
Country Admin / Sales Rep one-pagers). Which Loom account hosts them so
links survive long-term?
Recommendation: para.group.n8n@gmail.com master account — survives any
future agency-out handover. OK?

---

Then for the cutover call itself, I'll need ~30 min of synchronous time
with you for:

1. Supabase Auth SMTP wiring (Resend) — one dashboard config; raises
   sending limits from 2/hr → 30+/hr so password resets work for the rollout.
2. Create `paratusgroup` GitHub org under para.group.n8n@gmail.com
   (3-min form on github.com).
3. Run provision-users.ts --country=MZ — sends Resend invites to
   Mozambique's contact list; we smoke-test from one agent + the country
   admin seat together.
4. Flip Mozambique form-side ingestion to production webhook — last step
   before the 24-48 h soak begins.

Reply with the Q1–Q8 answers and a 30-min slot that works for you and
we're shipping the pilot.

Thanks,
Gerhard
```

---

## Resume prompts

When you come back, paste **one** of these into a fresh Claude Code session:

### Path A — William has answered Q1–Q8 and you're ready to do the cutover ceremony

```
Resume from /Users/gerhardvandenheever/Projects/paratus-group-dashboards/.planning/handoff-2026-05-06.md.
William's Q1-Q8 answers are below; he's available for the cutover ceremony at <TIME>.
Run /gsd:execute-plan 07-03 in live-cutover mode.

Q1: <his answer>
Q2: <his answer>
... etc
```

### Path B — William hasn't replied yet, you want to keep working on non-William items

```
Resume from /Users/gerhardvandenheever/Projects/paratus-group-dashboards/.planning/handoff-2026-05-06.md.
William hasn't answered Q1-Q8 yet. Pick up the highest-leverage carry-over
that doesn't need him. Show me the options first before starting.
```

### Path C — Just want a quick status check, no work yet

```
Resume from /Users/gerhardvandenheever/Projects/paratus-group-dashboards/.planning/handoff-2026-05-06.md.
Brief me on where we are. No work yet — just status.
```

---

## Repo state at pause

- **Branch:** `main` at `49763e5`
- **Working tree:** clean (verified)
- **Origin:** in sync (verified)
- **Production:** deployed Ready (last deploy `paratus-group-dashboards-h7iga65ra-paratusgroup.vercel.app` post-`1d4b6a2`; `49763e5` was migration-only, no Vercel rebuild needed)
- **Supabase:** migration `00019_advisor_sweep` applied, recorded in `supabase_migrations.schema_migrations`. Tier 1 advisor surface clear (verified via `get_advisors`).
- **Tags:** `phase-1-complete` through `phase-6-complete` all on origin.

Nothing in flight. Nothing half-shipped. Safe to walk away.

Feel better.
