---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/eslint.config.mjs
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/app/(hq)/page.tsx
  - apps/web/app/(country-admin)/[country]/page.tsx
  - apps/web/app/(sales-rep)/[country]/queue/page.tsx
  - apps/web/public/logo.png
  - apps/web/public/favicon.ico
  - apps/web/.env.local.example
  - apps/web/.gitignore
  - package.json
  - turbo.json
  - packages/config/eslint/index.js
  - packages/config/typescript/nextjs.json
  - packages/config/tailwind/postcss.config.mjs
  - packages/ui/src/layouts/dashboard-layout.tsx
  - packages/ui/src/index.ts
autonomous: true
domain: nextjs+turborepo
---

<objective>
Scaffold the `apps/web` Next.js 16 app and wire it to the existing `@repo/ui`, `@repo/supabase`, and `@repo/config` packages so the design system renders end-to-end on three role-grouped placeholder routes.

Purpose: Phase 1's first deliverable — an empty-but-branded app that boots, type-checks, lints, and builds. Without this, nothing else in Phase 1 can land. No auth or data yet; this plan is pure shell + design system.
Output: A running `npm run dev` shows the Paratus-branded `DashboardLayout` rendering on `/` (hq), `/{country}` (country-admin), and `/{country}/queue` (sales-rep). `npm run type-check`, `npm run lint`, and `npm run build` all green from the repo root.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/roadmap.md
@.planning/handoff-2026-04-27.md
@CLAUDE.md
@STYLE_GUIDE.md
@PRD/technical.md
@PRD/features.md
@package.json
@turbo.json
@packages/ui/package.json
@packages/ui/src/index.ts
@packages/ui/src/styles/theme.css
@packages/ui/src/styles/globals.css
@packages/ui/src/layouts/dashboard-layout.tsx
@packages/ui/src/layouts/auth-layout.tsx
@packages/supabase/package.json
@packages/config/package.json
@docs/design-reference/hq-dashboard.html
@docs/design-reference/country-admin-dashboard.html
@docs/design-reference/sales-rep-dashboard.html
@docs/design-reference/logo.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/web Next.js 16 app and wire workspace dependencies</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs, apps/web/eslint.config.mjs, apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css, apps/web/.env.local.example, apps/web/.gitignore, apps/web/public/logo.png, apps/web/public/favicon.ico, package.json, turbo.json, packages/config/eslint/index.js, packages/config/typescript/nextjs.json, packages/config/tailwind/postcss.config.mjs</files>
  <action>
    Create the `apps/web` Next.js 16 application as a workspace package and register it with Turborepo.

    1. Create `apps/web/package.json`:
       - name: `web`, private, version 0.0.0
       - scripts: `dev` (`next dev --turbopack -p 3000`), `build` (`next build`), `start` (`next start`), `lint` (`next lint`), `type-check` (`tsc --noEmit`)
       - dependencies: `next ^16.2.2`, `react ^19.2.4`, `react-dom ^19.2.4`, `@repo/ui workspace:*`, `@repo/supabase workspace:*`
       - devDependencies: `@repo/config workspace:*`, `@types/node`, `@types/react`, `@types/react-dom`, `eslint ^9`, `eslint-config-next ^16.2.2`, `tailwindcss ^4.2.2`, `@tailwindcss/postcss ^4.2.2`, `postcss ^8`, `typescript ^5`

    2. `apps/web/next.config.ts`:
       - `transpilePackages: ['@repo/ui', '@repo/supabase']`
       - Production headers: CSP, HSTS (`max-age=63072000; includeSubDomains; preload`), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`. CSP must allow Supabase (`*.supabase.co`) and self; no `unsafe-eval`; `unsafe-inline` only on styles (Tailwind requires it).
       - `reactStrictMode: true`. No experimental flags unless required by the Next 16 baseline.

    3. `apps/web/tsconfig.json` extends `@repo/config/typescript/nextjs.json` (create that file if missing — base from `tsconfig/nextjs` with `strict: true`, `paths: { "@/*": ["./*"] }`).

    4. `apps/web/eslint.config.mjs` re-exports `@repo/config/eslint` (create or extend `packages/config/eslint/index.js` to compose `next/core-web-vitals` + `next/typescript`).

    5. `apps/web/postcss.config.mjs` re-exports `@repo/config/tailwind/postcss.config.mjs` (which exports `{ plugins: { '@tailwindcss/postcss': {} } }`). Tailwind v4 reads `@theme` from CSS — no `tailwind.config` file needed at the app level.

    6. `apps/web/app/globals.css` content (in this exact order):
       ```css
       @import '@repo/ui/theme.css';
       @import '@repo/ui/globals.css';
       ```
       Do NOT redefine theme tokens here — `@repo/ui/theme.css` is the single source of truth.

    7. `apps/web/app/layout.tsx`:
       - Imports `./globals.css`
       - Sets `<html lang="en">` with `suppressHydrationWarning`
       - Body uses `font-sans antialiased bg-background text-foreground`
       - Metadata: `title: 'Paratus Group Dashboards'`, `description: 'Lead pipeline command center for Paratus Group — 12 active African countries.'`, `icons: { icon: '/favicon.ico' }`
       - DM Sans loaded via `next/font/google` if available, exposed as the `--font-sans` CSS variable on `<html>`

    8. `apps/web/app/page.tsx` — temporary root that renders nothing for now (Plan 02 replaces with redirect-to-role logic). Return `null` and a `<noscript>This dashboard requires JavaScript.</noscript>`. No auth yet.

    9. Copy `docs/design-reference/logo.png` to `apps/web/public/logo.png`. Generate `apps/web/public/favicon.ico` from the same logo (use `sharp` via `npx` one-shot, or accept a PNG fallback in the same path — favicon.ico is required by Next).

    10. `apps/web/.env.local.example` (no real secrets):
        ```
        NEXT_PUBLIC_SUPABASE_URL=
        NEXT_PUBLIC_SUPABASE_ANON_KEY=
        SUPABASE_SERVICE_ROLE_KEY=
        ```
        And `apps/web/.gitignore` must include `.env*.local`, `.next/`, `node_modules/`, `.vercel/`.

    11. Update root `turbo.json` to add `lint` and `type-check` task definitions if missing (already present per current file — verify env passthrough includes `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` for `build`).

    12. Run `npm install` from the repo root to materialize the workspace links.

    AVOID:
    - Do NOT create a `tailwind.config.ts` inside `apps/web` (Tailwind v4 + `@theme` in `@repo/ui/theme.css` is the locked pattern from STYLE_GUIDE.md).
    - Do NOT add `@supabase/*` packages to `apps/web/package.json` directly — they live in `@repo/supabase` and are re-exported via the package's `exports` map.
    - Do NOT add Tailwind, Radix, or `lucide-react` to `apps/web/package.json` — they're transitively available through `@repo/ui` via `transpilePackages`.
    - Do NOT touch `packages/ui` source unless `packages/ui/src/index.ts` is missing an export this plan needs (then add only the missing export, nothing else).
  </action>
  <verify>
    - From repo root: `npm install` succeeds, no peer-dep warnings beyond the inherited `@supabase/ssr` peer notes.
    - `npm run type-check` clean across the monorepo.
    - `npm run lint` clean.
    - `npm run build` succeeds and produces `apps/web/.next/`.
    - `npm run dev` starts `apps/web` on `localhost:3000`; the root page returns 200 with no console errors (curl -I http://localhost:3000 shows `200`).
    - `apps/web/public/logo.png` and `apps/web/public/favicon.ico` exist and are non-zero bytes.
  </verify>
  <done>The Next.js 16 app boots, type-checks, lints, and builds. Globals pull the Paratus theme from `@repo/ui/theme.css`. No app routes render UI yet — that's Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Render three role-grouped placeholder pages with the branded DashboardLayout</name>
  <files>apps/web/app/(hq)/page.tsx, apps/web/app/(country-admin)/[country]/page.tsx, apps/web/app/(sales-rep)/[country]/queue/page.tsx, packages/ui/src/layouts/dashboard-layout.tsx, packages/ui/src/index.ts</files>
  <action>
    Create the three role-grouped route placeholders, each rendering `DashboardLayout` with a Paratus-appropriate sidebar variant. These pages are intentionally static — no data fetching, no auth (auth lands in Plan 02). Their job is to prove the design system renders the same way it does in AMA.

    1. Inspect the AMA reference at `~/Projects/ama-amacare-stats-callback-dashboard/apps/admin/app/(dashboard)/page.tsx` and `~/Projects/ama-amacare-stats-callback-dashboard/packages/ui/src/layouts/dashboard-layout.tsx` (read-only — do NOT copy AMA's domain content). Mirror the layout shape: sidebar (logo + nav items + user footer), top bar (page title + breadcrumb area), main content well.

    2. If `packages/ui/src/layouts/dashboard-layout.tsx` does not yet accept a `navItems` prop and a `currentPath` prop, extend it minimally to do so (no other refactors). Each `navItem` shape: `{ label: string; href: string; icon: LucideIcon; active?: boolean }`. Re-export from `packages/ui/src/index.ts`.

    3. Define a single `paratusNav.ts` helper in `apps/web/app/_lib/nav.ts` exporting three nav arrays:
       - `hqNav`: Overview, Countries, Service Mix, Settings
       - `countryAdminNav`: Overview, Pipeline, Agents, Leads, Settings
       - `salesRepNav`: My Queue, Today's Stats
       Each entry uses a `lucide-react` icon (LayoutDashboard, Users, BarChart3, Settings, Phone, Filter, etc.).

    4. `apps/web/app/(hq)/page.tsx`:
       - Server Component. No auth check yet (middleware lands in Plan 02).
       - Renders `<DashboardLayout title="HQ Overview" subtitle="Group-wide pipeline" navItems={hqNav}>` with a single `<SectionCard title="Phase 1 placeholder">Foundation in place. Phase 5 wires the live data.</SectionCard>` body.

    5. `apps/web/app/(country-admin)/[country]/page.tsx`:
       - Async Server Component. Reads `params: Promise<{ country: string }>` (Next 16 async params), destructures `country`.
       - If country isn't in the active list (Angola, Botswana, DRC, Eswatini, Kenya, Mozambique, Namibia, Rwanda, South Africa, Tanzania, Uganda, Zambia — encode as a `const` set in `apps/web/app/_lib/countries.ts`), call `notFound()`.
       - Renders `<DashboardLayout title={`${countryName} — Country Admin`} navItems={countryAdminNav}>` with a placeholder `<SectionCard>`.

    6. `apps/web/app/(sales-rep)/[country]/queue/page.tsx`:
       - Same shape as country-admin: async params, country gate via the same active-countries set, `notFound()` for unknown.
       - Renders `<DashboardLayout title="My Queue" subtitle={countryName} navItems={salesRepNav}>` with a placeholder `<SectionCard>`.

    7. Replace `apps/web/app/page.tsx` with a temporary `redirect('/hq')` — Plan 02 swaps this for role-based routing. (Use `redirect` from `next/navigation`.)

    8. Visual smoke check during dev: `npm run dev`, hit each route in a browser:
       - `localhost:3000/` → redirects to `/hq` → renders HQ layout
       - `localhost:3000/mozambique` → renders country-admin layout
       - `localhost:3000/mozambique/queue` → renders sales-rep layout
       - `localhost:3000/atlantis` → 404 (unknown country)

    9. Side-by-side check vs. `docs/design-reference/hq-dashboard.html`, `country-admin-dashboard.html`, `sales-rep-dashboard.html`:
       - Sidebar background `var(--sidebar)` (deep navy), accent orange `var(--accent)` for active nav row
       - Top bar height + spacing matches mockup
       - Body uses `var(--background)` token, not raw colours
       Note any drift in the SUMMARY for follow-up — but in this plan, fix anything that is a one-line tweak to `DashboardLayout` and leave structural drift for a later commit.

    AVOID:
    - Do NOT add data fetching, Supabase calls, or auth checks. Those are Plan 02.
    - Do NOT hand-code colour hexes — every colour comes from a theme token in `@repo/ui/theme.css`.
    - Do NOT import AMA components by path (`../../../ama-...`). Mirror via `@repo/ui` only.
  </action>
  <verify>
    - `npm run type-check` clean
    - `npm run lint` clean
    - `npm run build` succeeds
    - `npm run dev` then visit `/`, `/mozambique`, `/mozambique/queue`, `/atlantis`:
      - First three render `DashboardLayout` with the right title/sidebar
      - `/atlantis` returns 404
    - Browser DevTools: zero console errors on any of the three rendered routes
    - Visual diff vs. mockups: sidebar nav, header, content well within 1-2px of the design reference at desktop width (1440px). Mobile breakpoint not required this plan — that's Phase 3.
  </verify>
  <done>Three role-grouped placeholder pages render the Paratus-branded `DashboardLayout` with no data, no auth, and no console errors. Visual fidelity vs. mockups is in the "approved within minor spacing" band — anything bigger is logged in SUMMARY for Plan 02 to fix.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` clean from repo root
- [ ] `npm run type-check` clean
- [ ] `npm run lint` clean
- [ ] `npm run build` succeeds
- [ ] `npm run dev` boots on port 3000; HQ/country-admin/sales-rep placeholders all render
- [ ] No `console.log`, no `any` types, no TODOs, no half-wired UI
- [ ] Visual smoke check vs. `docs/design-reference/` — sidebar + topbar + content well match
- [ ] `apps/web/.env.local.example` documents the three Supabase env vars (placeholders only)
- [ ] `.gitignore` excludes `.env*.local`, `.next/`, `.vercel/`
</verification>

<success_criteria>
- Next.js 16 app exists at `apps/web` and is wired into the Turborepo workspace
- `@repo/ui` theme + components render correctly on all three placeholder routes
- `@repo/supabase` is installed but not yet exercised (Plan 02)
- `@repo/config` provides shared eslint + typescript + postcss configs
- Three role-grouped placeholder routes (`(hq)`, `(country-admin)/[country]`, `(sales-rep)/[country]/queue`) render the branded `DashboardLayout`
- All quality gates green: type-check, lint, build, dev boot
- Logo + favicon in `apps/web/public/`
- No dangling threads: every imported symbol is used, every config file is referenced, every placeholder page has a real (if minimal) body
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with frontmatter (phase: 01-foundation, plan: 01, subsystem: app-shell, requires: [], provides: [nextjs-app, design-system-wired, route-shell], affects: [01-02, 01-03], tags: [scaffolding, design-system], key-decisions: [tailwind-v4-no-app-config, transpile-packages-via-next-config], key-files: [apps/web/app/layout.tsx, apps/web/app/globals.css, apps/web/next.config.ts, apps/web/app/_lib/nav.ts, apps/web/app/_lib/countries.ts]) and sections: Accomplishments, Issues Encountered, Deviations from Plan, Visual Fidelity Notes (any drift to fix in Plan 02), Next Phase Readiness.
</output>
