---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/web/vercel.json
  - .vercel/project.json
  - apps/web/app/api/health/route.ts
  - .planning/PROJECT.md
  - README.md
  - SECURITY_CHECKLIST.md
  - PRD/milestones.md
autonomous: false
domain: vercel+nextjs
user_setup:
  - service: vercel
    why: "Phase 1 ships only when the app is live on Vercel from main and the same login → role-routing flow that worked locally also works on the deployed URL."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Same value as apps/web/.env.local — Supabase Dashboard → Project Settings → Data API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Same value as apps/web/.env.local — Supabase Dashboard → Project Settings → API Keys (anon)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Same value as apps/web/.env.local — Supabase Dashboard → Project Settings → API Keys (service_role). MUST be marked Sensitive in Vercel."
    account_setup:
      - url: "https://vercel.com/dashboard"
        skip_if: "Already signed into the Vercel project under para.group.n8n@gmail.com"
    dashboard_config:
      - task: "Add the Vercel preview/production URL to Supabase Allowed Hosts"
        location: "Supabase Dashboard → Authentication → URL Configuration"
        details: "After the first Vercel deploy, copy the production URL (e.g. https://paratus-group-dashboards.vercel.app) AND the preview URL pattern (https://*.vercel.app or the specific preview alias) into Site URL + Redirect URLs. Without this, auth callbacks fail in production with redirect mismatch errors."
      - task: "Enable Vercel Preview Deployment Protection"
        location: "Vercel Dashboard → Project → Settings → Deployment Protection"
        details: "Set to 'Vercel Authentication' or 'Password Protection' for Preview environments. Production stays public (login still gates the dashboard itself). This is a CLAUDE.md security requirement."
    local_dev:
      - "Run `vercel login` once to authenticate the CLI (it opens the browser). Sign in with the para.group.n8n@gmail.com Google account."
---

<objective>
Take the app live on Vercel from `main`, set production env vars, ship the deploy pipeline, and close the Phase 1 gate (docs + tag + side-by-side mockup check).

Purpose: Phase 1's final deliverable per `.planning/roadmap.md`: "Three placeholder pages render, branded sidebar, three test users land on the right page after login, deployed to Vercel." Plans 01 + 02 made it work locally. This plan makes it work in production and writes the closing-the-loop docs.
Output: A live `https://*.vercel.app` URL where each test user signs in and lands on their correct placeholder; PROJECT.md/Validated bullet flipped on for Phase 1; README quickstart confirmed; SECURITY_CHECKLIST re-run; git tag `phase-1-complete` on `main`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/roadmap.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@CLAUDE.md
@README.md
@SECURITY_CHECKLIST.md
@CREDENTIALS.md
@PRD/milestones.md
@PRD/technical.md
@docs/design-reference/hq-dashboard.html
@docs/design-reference/country-admin-dashboard.html
@docs/design-reference/sales-rep-dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Vercel — link project, set env vars, configure deploy, ship a /api/health endpoint</name>
  <files>apps/web/vercel.json, .vercel/project.json, apps/web/app/api/health/route.ts</files>
  <action>
    Use the Vercel CLI to link the existing Vercel project (already created under `para.group.n8n@gmail.com` per CREDENTIALS.md) to this monorepo and configure the deploy.

    1. Authenticate the CLI if needed: `vercel whoami`. If it errors with "Not authenticated", create an authentication checkpoint (see auth-gate pattern). Resume after `vercel login` returns the right account email.

    2. From repo root, run `vercel link` and select:
       - Scope: the `para.group.n8n@gmail.com` account / team
       - Project: existing project named `paratus-group-dashboards` (do NOT create a new one)
       - Confirm root directory as the monorepo root (Vercel auto-detects Turborepo). Build command Vercel infers from `turbo.json` should be fine; if Vercel asks, override Build Command to `npm run build` and Output Directory to `apps/web/.next`. Install Command: `npm install`.

    3. Pull current Vercel project config: `vercel env pull apps/web/.env.local --environment=development` — verify the three Supabase env vars are NOT yet set (we set them next). Discard the temporary file if it gets written.

    4. Set env vars for all three environments. Use `vercel env add` interactively (Claude pauses and waits for the user to paste values — do NOT echo them in chat). For each of `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`:
       - Production: same value as local `.env.local`
       - Preview: same value (single Supabase project, same data)
       - Development: same value
       - Mark `SUPABASE_SERVICE_ROLE_KEY` as Sensitive (Vercel asks during `env add`; if it doesn't, set it via dashboard after).

    5. Create `apps/web/vercel.json`:
       ```json
       {
         "$schema": "https://openapi.vercel.sh/vercel.json",
         "framework": "nextjs",
         "regions": ["fra1"]
       }
       ```
       (`fra1` = Frankfurt — closest to African users at v1; revisit per-country edge optimisation in Phase 6.)

    6. Create `apps/web/app/api/health/route.ts`:
       - GET handler. Returns `{ status: 'ok', commit: process.env.VERCEL_GIT_COMMIT_SHA ?? 'local', ts: new Date().toISOString() }` with 200.
       - No auth required. Listed in middleware excluded paths (already added in Plan 02 — verify).
       - This is the synthetic-monitoring target Phase 6 will hook up.

    7. `.vercel/project.json` is auto-written by `vercel link`. Confirm it's git-ignored (it is via `.gitignore`'s `.vercel` entry from Plan 01). Do NOT commit it.

    AVOID:
    - Do NOT run `vercel --prod` from the CLI yet. Production deploy comes from a `git push origin main` (matches CLAUDE.md "pushing to main triggers Vercel deployment").
    - Do NOT bypass `vercel env add` and write secrets in any committed file.
  </action>
  <verify>
    - `vercel whoami` returns the para.group.n8n account
    - `vercel env ls` shows the three Supabase keys present in production / preview / development
    - `apps/web/vercel.json` valid JSON, schema-correct
    - `apps/web/app/api/health/route.ts` responds 200 with the expected JSON locally (`curl localhost:3000/api/health`)
    - `.vercel/project.json` exists locally but is NOT staged in git
  </verify>
  <done>Vercel project linked, env vars set across all environments, health endpoint shipped, vercel.json in place. Ready to push to main.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger production deploy via main push, then re-run SECURITY_CHECKLIST and update docs</name>
  <files>.planning/PROJECT.md, README.md, SECURITY_CHECKLIST.md, PRD/milestones.md</files>
  <action>
    Ship the deploy and close every Phase 1 gate.

    1. Confirm `git status` is clean of untracked secrets. Stage and commit Plan 03's code changes (vercel.json + health route + any Plan 02 follow-ups not yet committed). Commit message:
       ```
       feat(01-03): wire Vercel deploy, /api/health, finalise Phase 1
       ```

    2. Push to `origin main`. This triggers the first production Vercel deploy. Wait for completion: poll `vercel ls --prod` (or `vercel inspect <url>`) until the latest deploy is `Ready`. If the deploy errors:
       - Read the build log via `vercel logs <deployment-url>` and fix the underlying issue (no `--no-verify` shortcuts, no env-var hacks).
       - Re-commit + re-push.

    3. After deploy goes Ready, capture the production URL (e.g. `https://paratus-group-dashboards.vercel.app`).

    4. **Stop here and create the verification checkpoint (next task).** Once approved:

    5. After approval, update docs (no code changes from this point):
       - `.planning/PROJECT.md` § Requirements / Validated:
         - Tick `Phase 1 — Foundation` and add a one-line "Validated 2026-XX-XX: signed in as each role on production URL, role routing green."
       - `README.md` "Getting Started":
         - Confirm the dev quickstart (`npm install` → copy `.env.local.example` → `npm run dev`) is correct against the as-built reality. Fix any drift.
         - Add a "Live URL" line pointing to the Vercel production URL.
       - `SECURITY_CHECKLIST.md` — re-run every applicable item and tick:
         - RLS enabled on `user_roles` (verified)
         - `service_role` not in any `NEXT_PUBLIC_*` (verified by `grep -r 'NEXT_PUBLIC_.*SERVICE' apps/ packages/` returning empty)
         - `auth.uid()` and JWT claims used in policies (no client-supplied IDs)
         - Security headers present in production (`curl -I <production-url>` shows CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy)
         - Vercel Preview deployments protected (verify in dashboard)
         - `.gitignore` covers `.env*`, `.vercel`, secrets — `git ls-files | grep -E '\\.env|\\.vercel'` returns empty
         - Supabase Allowed Hosts restricted to localhost + production URL
       - `PRD/milestones.md` § Phase 1: tick every checkbox; add "Validated YYYY-MM-DD" stamp in the milestone header.

    6. Tag the release: `git tag phase-1-complete && git push origin phase-1-complete`. Do NOT use `--force`. If the tag exists (it shouldn't — first phase), abort and ask.

    AVOID:
    - Do NOT push to main while a deploy is mid-flight (creates noisy duplicate deploys). Wait for Ready first.
    - Do NOT mark Phase 1 validated in PROJECT.md until the human-verify checkpoint passes against the production URL — local-only verification is not enough.
  </action>
  <verify>
    - `vercel ls --prod` shows latest deploy `Ready` from `main`
    - `curl -I <production-url>` returns 200 (or 307 to /login — both are fine as long as headers are present) AND headers include CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy
    - `curl <production-url>/api/health` returns `{ status: 'ok', commit: '<sha>', ts: '<iso>' }`
    - `git ls-files | grep -E '\\.env|\\.vercel'` returns empty
    - `grep -r 'NEXT_PUBLIC_.*SERVICE' apps/ packages/ --include='*.ts' --include='*.tsx' --include='*.json'` returns empty
    - `git tag -l phase-1-complete` shows the tag
    - Each Phase 1 milestone checkbox in `PRD/milestones.md` is ticked
    - `.planning/PROJECT.md` shows Phase 1 in Validated, not Active
  </verify>
  <done>App is live on Vercel from `main`, production smoke test green, every Phase 1 milestone ticked, security checklist re-run clean, docs updated, tag pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 — Foundation, complete and live. Three placeholder pages on the production Vercel URL, branded sidebar, three test users land on the right route after login, security headers in place, health endpoint responding, docs updated, and `phase-1-complete` tag on `main`.
  </what-built>
  <how-to-verify>
    Replicate the local Plan 02 checkpoint against the **production** URL. Do this in a clean browser / incognito window.

    1. Visit the production URL → 302 to `/login`.

    2. Sign in as `hq-test@paratus.group` → land on `/` (HQ Overview placeholder, branded sidebar). Visit `/mz` → renders. Visit `/mz/queue` → renders. Logout returns you to `/login`.

    3. Sign in as `country-admin-test@paratus.group` → land on `/mz`. Try `/ke` → bounced to `/mz`. Logout works.

    4. Sign in as `agent-test@paratus.group` → land on `/mz/queue`. Try `/ke/queue` → bounced to `/mz/queue`. Logout works.

    5. `curl -I <production-url>/`:
       - 200 or 307
       - `content-security-policy: ...` present
       - `strict-transport-security: max-age=63072000; includeSubDomains; preload`
       - `x-frame-options: DENY`
       - `x-content-type-options: nosniff`
       - `referrer-policy: strict-origin-when-cross-origin`

    6. `curl <production-url>/api/health` → `{ status: 'ok', commit: '<7+ char sha>', ts: '...' }`

    7. Visual side-by-side at desktop width (1440px):
       - HQ placeholder ↔ `docs/design-reference/hq-dashboard.html` — sidebar, top bar, content well within ±2px / same colours / same logo
       - Country-admin placeholder ↔ `docs/design-reference/country-admin-dashboard.html`
       - Sales-rep placeholder ↔ `docs/design-reference/sales-rep-dashboard.html`
       (Mockups have full content; placeholders are minimal — only the chrome must match. Drift in the chrome itself = blocker.)

    8. Browser DevTools: zero console errors, zero failed network requests.

    9. The Boil-the-Ocean reaction test: when you look at this in production, is the response "yeah, looks fine" or "holy shit, that's done"? Be honest. If the former, list what's missing.

    If everything passes, type "approved" — I'll write the SUMMARY and we're done with Phase 1. If anything fails, describe the failure and I'll diagnose / fix before tagging.
  </how-to-verify>
  <resume-signal>Type "approved" if every step passes against the production URL. Otherwise list failures.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `vercel ls --prod` shows latest deploy `Ready` from `main`
- [ ] Production URL responds with all security headers
- [ ] All three test users complete the role-routing flow on the production URL (verified by human)
- [ ] Cross-country redirects work in production
- [ ] `/api/health` returns 200 with commit sha
- [ ] No secrets in git (`git ls-files | grep -E '\\.env|\\.vercel'` empty)
- [ ] No `NEXT_PUBLIC_*SERVICE*` in any committed file
- [ ] `.planning/PROJECT.md` shows Phase 1 Validated
- [ ] `PRD/milestones.md` Phase 1 fully ticked
- [ ] `SECURITY_CHECKLIST.md` re-run end-to-end and ticked
- [ ] `README.md` quickstart matches as-built reality
- [ ] `phase-1-complete` tag on `main`
- [ ] No `console.log`, no `any`, no TODOs, no half-wired UI in any committed file
</verification>

<success_criteria>
- Live production URL serves the dashboard with the same role-routing behaviour as local
- All security headers present in production responses
- Vercel Preview Deployment Protection enabled
- Supabase Allowed Hosts restricted to localhost + production
- All Phase 1 milestone checkboxes ticked
- `phase-1-complete` tag pushed
- Visual fidelity vs. mockups passes the side-by-side check
- Zero console errors anywhere
- Phase gate per `.planning/roadmap.md` § "Phase Gates — Boil the Ocean Standard" is satisfied: code shipped, tests green, docs updated, demo runnable, security checklist re-run, visual fidelity check, commit + tag, no dangling threads
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with frontmatter (phase: 01-foundation, plan: 03, subsystem: deploy, requires: [01-01, 01-02], provides: [vercel-deploy, production-url, security-headers, health-endpoint, phase-1-tag], affects: [02-*, all-future-phases-deploy-pipeline], tags: [deploy, security, phase-gate], key-decisions: [vercel-region-fra1, single-supabase-project-across-envs, main-as-prod-trigger], key-files: [apps/web/vercel.json, apps/web/app/api/health/route.ts, .planning/PROJECT.md]) and sections: Accomplishments, Issues Encountered, Deviations from Plan, Production URL, Phase 1 Gate Closure (every gate item with verification evidence), Next Phase Readiness (must explicitly handoff to Phase 2: ingestion endpoint, RLS migrations 00002+, country-scoped policies — and confirm the JWT `country_code` claim is the tenant key Phase 2 will use).

Then create `.planning/phases/01-foundation/PHASE-SUMMARY.md` rolling up Plans 01 + 02 + 03 into the Phase 1 closure note (link the three plan summaries, list every milestone ticked, embed production URL, embed `phase-1-complete` tag SHA).
</output>
